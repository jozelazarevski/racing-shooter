{
  "name": "ignite-rally-ios",
  "private": true,
  "version": "1.0.0",
  "description": "iOS App Store shell for IGNITE RALLY. The game itself lives in the repo root; scripts/build-www.mjs copies it into www/ and Capacitor wraps that in a WKWebView app.",
  "scripts": {
    "build": "node scripts/build-www.mjs",
    "sync": "node scripts/build-www.mjs && cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "sharp": "^0.33.0"
  }
}

# RALLY_RULES.md — the owner's rules, all of them, in one place

**Authority.** CLAUDE.md names this file the top of the chain:
`RALLY_RULES.md (race outcomes) > RALLY_DRIVING.md (vehicle physics constants)
> CLAUDE.md > code`. Until 2026-09-09 the file did not exist, so the top of
the chain was a dangling reference and every rule had to be re-derived from
CLAUDE.md prose, HANDOVER narrative, or the code. That is the defect this
file closes.

**What is in here.** Every OWNER DIRECTIVE, verbatim where the words are on
record, with the operational rule it became and its current status. Specs
that are documents in their own right (RALLY_MASTER_SPEC.md,
RALLY_RACE_INTEGRITY.md, RALLY_W_CURVE_01.md) are cited, not copied — but
every rule they contain that CONFLICTS with something else is recorded in
§K, which is the section to read before implementing anything.

**How to use it.**
1. Cite rules by ID (`R-3.2`, `HRD-7`, `AI-2`). IDs never change meaning; a
   superseded rule keeps its ID and gains a SUPERSEDED-BY line.
2. Before writing code that touches a rule, check §K for a conflict on it.
3. A new owner sentence goes in here FIRST, with a status of OPEN, before
   any code is written against it.
4. Never resolve a conflict silently. Record it in §K with the resolution
   and the reason, or leave it in §L for the owner.

**Status vocabulary.** LIVE (shipped and deployed) · BUILT (in the tree,
not yet deployed) · OPEN (agreed, not yet built) · HELD (blocked on an
owner decision, see §L) · SUPERSEDED.

---

## A. Standing decisions — do not reopen

| ID | Rule | Status |
|---|---|---|
| A-1 | The world is fully drivable. No invisible walls, no rule-based speed caps, no return forces. Containment is physics (slope, surface, gravity, terrain collision) and race structure (gates). | LIVE |
| A-2 | Repair only for UI: no new on-screen elements, no world overlays. The r297 yellow centreline and rival arrows are erased with no replacement. Scope LIFTED for world and track geometry by RALLY_PATCH_02 v3 C-4. | LIVE |
| A-3 | The in-race HUD is frozen exactly as shipped in recording E. Nothing moves, resizes, restyles or is removed. Toast BEHAVIOUR may change; toast APPEARANCE may not. | LIVE |
| A-4 | No minimaps, ever, in any corner or mode (RULES.md §0.1; removed twice). Any request that seems to imply a map is re-checked against this first. | LIVE |
| A-5 | No route lines, arrows or braking markers. Corner readability comes from world design: scenery framing, tree lines tightening into curves, containment as natural signposting. | LIVE |
| A-G | "Folow the graphics I send" (2026-09-10) | AN OWNER FRAME IS THE BRIEF, NOT AN ILLUSTRATION. Where the owner sends a picture with a request, that image is the target and the spec: match what it shows rather than what the words could also mean. This is already how r395 (the mountain-flank reference frame) and r393 (the tree design sheet) were built, and it is why the white-line hunt was worth five rounds — the frames were right and the reading of them was wrong. A frame also OUTRANKS my inference: if a picture and a rule disagree, raise the conflict in §K, do not quietly follow the rule. | LIVE |

## B. Physics and containment

| ID | Owner sentence / source | Rule | Status |
|---|---|---|---|
| B-1 | CLAUDE.md §3.1 | Terrain is a heightfield; overhangs/tunnels/arches are separate closed trimeshes. Validator rule. | LIVE |
| B-2 | §3.3 | Above `maxClimbDeg` 35° a wheel produces no drive and lateral grip decays to 0 over 0.5 s — the car slides down rather than hanging. | LIVE |
| B-3 | "Should be wrecked at this kind of falls" (r320, PIKES PEAK shelf) | Falls past the damper-priced free band cost hull. | SUPERSEDED-BY B-4 |
| B-4 | RALLY_PATCH_02 v3 FIX-7 | A cliff fall costs −1 hull retained, respawns on the road unfrozen with 3 s invulnerability, camera stays on the player. DESTROYED by terrain alone MUST be unreachable in a normal run. | LIVE |
| B-5 | "Rule: when I get out of the cliff I need to drop immediately" (2026-09-09) | Leaving a lip hands the car to gravity on the same frame. MEASURED CAUSE: `fallEdgeDrop` is 3 m, so the car rappels the first 3 m at up to 11 u/s before going ballistic. The code's own note gives the principled value — below ~0.9 u the ground-follow ease is already inside its cap — so the threshold is ~1.0. | OPEN |
| B-6 | "Car should not drive between the trees" (§7.15) | A forest stand is a MASS: trunks stop a car like any solid, and dense stands carry heavy underbrush drag. Physics, never an invisible wall. | LIVE |
| B-7 | "Rule: I can't drive through a tree" (2026-09-09), RE-REPORTED "Should not be able to drive in the trees - rule" (2026-09-10) — the third owner sentence on this, after r399's "Car should not drive between the trees". Three sentences means the r399 build did not close it and this is now a build, not a backlog row. | Every tree the car can reach is solid. THREE MEASURED GAPS: (a) the forest carpet registers colliders for the VERGE ring only. LOCATED r410: `_buildForestCarpet` calls `ring(...)` four times and only the verge call passes its trailing `reg = true`, so `camTrees` holds the 9,000 verge instances of `trackSpot(1, 38)` and nothing else. The mid ring is `trackSpot(38, 160)` at 14,000 instances with `reg` omitted — an uncollided band 38 to 160 u out from the road edge, on every carpet world, which is exactly the belt a car reaches when it leaves the road. The horizon ring is `radialSpot(170, maxR+450)` at 24,000. BOTH the trunk colliders and the 7.15 underbrush drag read `camTreesNear`, so in that band there is neither a trunk to hit nor brush to bog in: the r399 build made the VERGE a mass and left the forest behind it paint, which is why the owner can still drive in the trees; (b) a trunk hit at ≥16.7 m/s keeps 60% of approach speed (FIX-5, r388) on BOTH collision paths — which contradicts B-6, B-8, and §7.15's own shipped words ("no 60%-and-through deflection"). The rule text and the code disagree today; (c) `this.trees` carries `solid:false` on saplings, snags, palms, vines, cacti and small oaks. | OPEN |
| B-8 | "Rule: I can't push trees" (2026-09-09) | No tree yields, is felled, or moves. Deletes the smash/shove class FOR TREES. CONSEQUENCE TO ACCEPT: tree-smash scoring goes with it (§1 "smash and shove props with score"). Vines (`kind: 'vine'`, hanging foliage, not a trunk) stay non-solid or the jungle gains mid-air walls. | OPEN |
| B-9 | "This should not be allowed." (2026-09-10, phone frame, r409 BRIDGE RUN) | A CAR MAY NOT DRIVE ALONG A STEEP BANK. The frame shows the car OFF the carriageway on the grass embankment beside a guard-railed road, nose down the fall line, holding 31 km/h and TRAVERSING the slope rather than sliding off it — a rival truck is doing the same lower down, so this is not a player-only quirk. §3.3 already says that above `maxClimbDeg` (35°) a wheel makes no drive and its lateral grip decays to zero over 0.5 s "so the car slides down instead of hanging". That law is written for CLIMBING; this is CROSS-SLOPE, and the frame is the evidence it does not bite there. THE RULE: on ground steeper than `maxClimbDeg`, a car loses lateral hold in every direction, not only uphill — it slides down the fall line and cannot hold a line across the face. Off-road is still never a fault (C-1, C-2): the car slides, it is not reset. NOT YET DIAGNOSED — measure the actual slope angle under that car before touching the law, because if the bank is under 35° the fault is the terrain, not the physics. | OPEN | STATIC READ OF THE PHYSICS, 2026-09-10 (a hypothesis, NOT yet measured — the bank angle in the frame is still unmeasured). The "face sheds the car" law exists and is correct (vehicles.js ~2103, r330): past `maxClimbTan` gravity pushes the BODY downhill, and it deliberately triggers on the TRUE gradient magnitude, not the heading-projected grade, precisely so "a car parked sideways across a 60 deg face" is caught — which is this frame's geometry. IT DOES NOT FIRE HERE BECAUSE OF A FENCE, NOT A GAP IN THE MATHS: the whole block is gated on `farOffRoad`, which needs `|lateral| > rejoinBandU` (34 u) or 34 u from the tracked centre. The r328 fence was added on the reasoning that within that band "banks are moments, not faces". The owner's frame is a car on a bank INSIDE that band, and says it should not be allowed — so the fence, not the law, is the defect. THIS IS A CONFLICT WITH A DELIBERATE PRIOR DECISION and is filed as K-14, not silently traded: the r328 fence protects the rejoin (a car bouncing off a verge to get back on the road must not be shed mid-bounce), so the fix is to NARROW the fence, not delete it — e.g. shed inside the band too once the grade is well past the ceiling AND the car has held the face longer than a bounce. Also note `slopeAuth` already zeroes drive above 35 deg, so a car crossing at 31 km/h inside the band is COASTING on momentum with grip it should not have; the lateral-decay half of §3.3 is the other thing to check once the angle is measured.

## C. Recovery and resets

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| C-1 | "Don't reset the car when I go off route" (r345, §3.6c) | The player's missed-gate return is DELETED. Leaving the route costs progress, never the car. The owed gate stays armed; driving back through it clears the debt. Rivals keep their off-course recovery — a parked rival is a bug. | LIVE |
| C-2 | "Don't reset me when I am off-road" (r364, §3.6d) | The cliff-top auto-return is DELETED. Being off-road, however high, is never a fault. Only physical traps rescue: under-terrain/void, wedged under held throttle, bogged on wrong tyres, upside down, fatal falls, water. | LIVE |
| C-3 | "When car restarts after selecting goes straight to nitro. Should be stopped." (r324, §3.6b) | A restarted car STANDS: `returnSpeedKmh` 0, and every placement zeroes a burning boost. Heading-on-tangent still applies. | LIVE |
| C-4 | R-RECOVER-01 (Race Integrity) | Off-road, held throttle, under 25 km/h and under 8 m of along-track progress in the window is STUCK and is rescued. On the road the 1 m bar stands. Gated on held throttle and on being off-road, which is what keeps C-1 and C-2 intact. | BUILT (r408) |
| C-5 | R-RECOVER-01 trigger 2 | Off-road AND >15 m from the spline for 2 s → auto-reset. | HELD — see L-1 |
| C-6 | R-RECOVER-01 reset behaviour | 40 km/h rolling start. | HELD — see L-2 |

## D. Race outcomes

| ID | Rule | Status |
|---|---|---|
| D-1 | Lap counter starts at 1. Start-line trigger inert until checkpoint 1 is passed; no event, no message. (§6.1) | LIVE |
| D-2 | "I don't need 3 laps. Race is one lap only." (r364) — default lap count 1; a world's own `laps` still wins. | LIVE |
| D-3 | "Decide when track is 1 or 3 laps depending the length." (r381) — under `lapsShortTrackU` (3000 u) races 3 laps, longer races 1. | LIVE |
| D-4 | R-FINISH-01: crossing the finish MUST end the race within 500 ms, in every vehicle state. MEASURED CAUSE: there is no trigger volume — the finish is an index wrap gated on a checkpoint mask, and r311's gate-0 guard seated a return ON the line, so the teleport CONSUMED the crossing. Fixed by seating before the line (carrying lap and wrap counts back with it) and by giving every placement two frames of immunity from both wrap tests. | BUILT (r408) |
| D-5 | Kills affect position: a destroyed rival respawns at its last gate after a 4.0 s hold. (§6.6) | LIVE |
| D-6 | "The cars start really strange. I don't see an order how do I win 1st place or 4th place. Also they all start super far away. Unusual for a race." (2026-09-09) — THE GRID IS MEASURED IN METRES. `gridSlot` spaced rows by `row * 8` centreline SAMPLES, written when a sample was a couple of metres; since the r340 length doubling `segLen` is 6.1-13.4 u across the roster, so eight samples is 53-77 m a row. FALKEN RIDGE measured 77 m per row, 230 m pole-to-back, pole itself 96 m up the road from the line. Now `gridRowGapM` 9 / `gridPoleBackM` 8 / `gridLateralU` 3.6 in driving.json, converted through each world's own segLen (rounded, floor one sample so two cars sharing a lateral can never share a station). Measured after, ten worlds: rows 6.1-13.4 m, depth 18-40 m, eight distinct slots everywhere (FALKEN RIDGE 10 m rows, 29 m depth). | BUILT (r409) |

## E. World and track

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| E-1 | "All Tunels needs to be under a mountain otherwise makes no sense." (r350) | Every bore lies under real terrain mass end to end, flank overburden ≥25 m. | LIVE |
| E-2 | "Olive coast make it like small hills next to the sea." (§7.16) | On a coast world the water MUST be visible from the coast road; the band between road and waterline is rolling knolls. Acceptance is the sightline census, not the eye. | LIVE |
| E-3 | "Lower the height difference across the game. Instead of the straight climb introduce more interesting snake turns going up and down." (§7.17) | The ROAD's height range per lap is modest; a sustained climb is SNAKED and undulating, never a straight ramp. | OPEN (build reverted, see L-4) |
| E-4 | "Add more curves and jumps" (2026-09-09) | Raise corner density and kicker count roster-wide within §8 template bounds, W-CURVE-01 and T-04. | OPEN |
| E-5 | W-CURVE-02 (Race Integrity) | No segment may hold the car at top speed with zero steering for more than 12 s. Observed 45 s at 250 km/h. | OPEN |
| E-6 | Speed budget (§6.3, §7.5) | `nitroCeilingKmh = min(designSpeedKmh + 20, gearTop + 40)`. NOTE: 250 km/h was observed on an alpine world whose design speed is 160, so the ceiling should be ~180 — a breach the Race Integrity patch does not itself flag, and one that makes E-5 worse than it looks. | OPEN |
| E-7 | W-EDGE-01a (Race Integrity) | A drop beside the road needs containment along its whole length. MEASURED PROBLEM: at the patch's own numbers (>2 m within 4 m of the edge) FALKEN RIDGE — where both observed falls happened — has ONE violating station in 900, because §7.11 deliberately sets drops back beyond 3 m of verge. Band sweep on that world: 4 m → 1 station; 8 m → 299, longest run 1005 m; 15 m → 460, longest run 1771 m. The band must be derived from how far a car actually travels sideways after a hit at racing speed, not picked. | OPEN — see L-3 |
| E-8 | "This as well" (2026-09-10, second phone frame, r409 BRIDGE RUN) | AN UNGUARDED CLIFF LIP ALONGSIDE A FLAT-OUT SURFACE. The frame shows the car at 199 KM/H on the broad tan shoulder, with a sheer cliff falling away immediately to the left. Containment along that lip is ONE TRAFFIC CONE. The previous frame from the same world had guard rails; this stretch has none. Three rules already cover pieces of this and none of them fired here: §7.11 says the drop side MUST NOT be reachable at speed from the racing line without crossing at least 3 m of verge; §7.9 says the last 2 m before a drop uses the rock material, not grass, and the lip in this frame is unmarked ground; C-3 as amended permits BREAKABLE guardrails on any drop over 15 m. THE RULE: where the drivable surface runs beside a drop, it carries containment — breakable rail, or a verge wide enough that the lip is not reachable at speed — AND the last 2 m reads as an edge. A cone is not containment. THIS FRAME SETTLES K-7: three different drop thresholds are on the books (W-EDGE-01a >2 m within 4 m, the shipped rail builder 2.5 m at a single 7.0 m probe, §7.11 any step over 0.15 m) and the reason this lip has nothing is almost certainly that the rail builder's single probe missed it. Measure the drop and the probe before writing the fix. | OPEN | STATIC READ OF THE BUILDER, 2026-09-10 (a hypothesis, NOT yet measured on the world in the frame). `_buildEdgeRails` (track.js:13689) decides "is there a fall here" from ONE SAMPLE at ONE fixed lateral: `out = pointAt(i, (half + 7.0) * side); drop = center[i].y - terrainHeight(out.x, out.z)`, rail only if `drop >= 2.5`. A single point 7 u out cannot see a lip whose fall begins FURTHER out than 7 u — on a world with a broad shoulder (and the frame shows exactly that, a wide tan shoulder with the cliff beyond it) the probe lands on level verge, reads ~0, and the whole stretch is dropped before the tight-corner rule or the run detector ever look at it. A shelf at 7 u bracketing a notch nearer in fails the same way. Candidate fix shape: sample a RANGE of laterals across the reachable verge (roughly half+2 to half+14) and take the MAX drop, so any lip inside the belt a car can reach arms the rail. MUST BE MEASURED FIRST on the world in the frame — how far out the lip actually sits — and it will add rails roster-wide, so MAXBAY (420) and the `barriers` per-frame walk are both in scope. This also settles K-7 once measured. AND THE SUITE SHARES THE BLIND SPOT, WHICH IS WHY THE GATE IS GREEN ANYWAY. W-CURVE-01.4 in `test-stagerules.mjs` (~line 289) asks "do sharp drop curves carry a barrier within 8 u" using THE IDENTICAL SINGLE-POINT PROBE — `out = pointAt(i, (half + 7.0) * s)`, skip the station if the drop there is under 2.5 u. So the check that is meant to catch an unguarded lip cannot see any lip the BUILDER cannot see: both ask the same question at the same one point, and a station whose fall starts beyond 7 u is silently dropped from the census rather than failed. A SECOND filter narrows it further: the loop begins `if (radE(i) > 30) continue`, so only SHARP CURVES are examined at all — a straight or gentle bend beside a cliff is never checked, and the frame shows 199 km/h, which is not a sharp curve. CONSEQUENCE, and it is the point: a green W-CURVE-01.4 is NOT evidence that drops are guarded, and no past green should be cited as if it were. The fix must move BUILDER AND SUITE TOGETHER (a belt-sampled max drop in both, and the curvature filter widened or dropped); fixing only the suite would turn it red roster-wide, which would be CORRECT but is a large separate piece of work that must be sized before it is started.
| E-9 | "Add hazards and obstacles to Dodge and blow up across the tracks" (2026-09-10) + "Yes. Folow the graphics I send" + THE HAZARD FRAME (2026-09-10, an annotated r409 CANYON RUN capture) | THE FRAME IS THE BRIEF (A-G). What it shows, read as the spec, is SIX bodies and one effect: (1) EXPLOSIVE / DANGER CRATES — red-painted wooden crates with a stencilled hazard legend, singly and in stacks of two to three, standing on the verge and on rock ledges beside the road; (2) FUEL / GASOLIN BARRELS — red drums in clusters of three to five, verge and ledge, the same class as the crates; (3) DYNAMITE BUNDLES strapped to the cacti — the existing scenery body carries the charge, no new mesh; (4) CHARGE PLATES laid FLAT ACROSS THE DIRT — the frame puts these squarely in the carriageway; they are new, and HRD-1a bounds them (the crates and drums do NOT need that amendment, since the pass-through prop class is already on the surface roster-wide); (5) FLOATING RED TARGET MARKERS over the rock arches — HELD, raised as K-13 against standing decision 2, not built with the rest; (6) EXPLOSION BURSTS — VFX on destruction, the existing debris path. Classes: 1-3 are DESTRUCTIBLE, off the driving surface, priced as smash/shove and scored; 4 is the new low-profile class bounded by HRD-1a; plus the DODGEABLE arrivals E-9 already carried (rockfall, rolling boulders, crossing traffic) which stand in nobody's way until they arrive. Density and placement otherwise follow §7.3 unchanged, and kicker landing zones stay clear. | SPEC'D, NOT BUILT |

## F. Plants, scenery and structures — the HRD hard road rules

HRD-1..6 are recorded in CLAUDE.md §7A and enforced in the blocking gate.
Summarised here for citation; that table remains the operational text.

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| HRD-1 | "RULE: nothing stands at the middle of the road" | No body of any class intersects the drivable width at any station, on any leg. Gate: test-nothing-on-road. | LIVE |
| HRD-1a | AMENDMENT to HRD-1, on the E-9 hazard frame (2026-09-10) and the owner's "Yes" to literal blockers | MEASURED FIRST, AND IT CHANGED THE AMENDMENT. HRD-1 was never enforced against everything: `_buildProps` already stands CRATES, CONES AND BARRELS on the drivable surface across the whole roster, on purpose — "NOT blockers, a car drives straight through one and accelerates" — and `test-nothing-on-road` COUNTS them and prints the total rather than asserting against them. So the operative law, already in force and only now written down, is: HRD-1 forbids a body that STOPS THE CAR. A pass-through smashable is not one, and the frame's crates and drums are exactly that class. THE AMENDMENT IS THEREFORE A BOUND, NOT A HOLE: any body inside the drivable width MUST (a) be destructible and yield on contact — no velocity stop, no hard collision response, the car leaves faster than a blocker would let it; (b) stand <= 0.6 m for a smashable, <= 0.25 m for a CHARGE PLATE, so nothing lifts a wheel or hides behind a crest; (c) be telegraphed at least 60 m ahead on the racing line, readable at the world's design speed on every palette (§7.9); (d) never sit in a gate clear zone, an apex band, a kicker landing zone or the finish run-out; (e) charge plates at most one per 150 m of road. Everything else HRD-1 forbade in the carriageway still fails hard — a house, a rock, a tree, a fence, a sign, a barrier, anything that stops a car. `test-nothing-on-road` gains the charge plate in the SAME shape as the existing prop class: counted, with (a)-(e) asserted as bounds. No by-name pin and no waiver. | SPEC'D, NOT BUILT |
| HRD-2 | "This road needs widening" | Curvature widening, bounded by W-CURVE-01.2 (±30% of approach width). | LIVE |
| HRD-3 | — | Drivable half-width never below 3.0 u. | LIVE |
| HRD-4 | — | `|widthAt(i+1) − widthAt(i)| ≤ 0.6 u` — the road tapers, never steps. | LIVE |
| HRD-5 | "there is always a mountain range from one of the sides of the road", scoped to "steep mountains… there can be field roads. That is fine." | On steep-mountain terrain every station carries rising ground on at least one side. | LIVE |
| HRD-6 | "No road floats or is on a ridge" | No station has falling ground on both sides; no road edge stands unsupported. | LIVE |
| HRD-7 | "Fix all trees that are in the house — this is A HARD RULE — swipe across whole game" (2026-09-09) | No tree, bush or tuft may stand inside a structure footprint, on any of the 78 worlds. The plant loses, not the building. MEASURED: 41,195 offenders across 67 of 78 worlds — 36,329 forest carpet, 4,619 ground cover, 247 solid stand. Worst SEA CLIFF RUN 16,322 and CLIFF KNOT 14,622. Needs a blocking-gate suite, as a hard rule. | OPEN |
| F-1 | "Pine trees are never not green… Apply this across the game." (§7.14) | Every conifer renders green foliage on every palette. LARCH IS EXEMPT by the owner's own word. | LIVE |
| F-2 | "Trees in the ice?? Fix" (2026-09-09, GLACIAL PASS) | Nothing grows inside a cliff ribbon on any cliffWalls world, and on the ice-walled worlds nothing grows in the canyon or up its ice flanks. Snowy forests (FROST PEAK) are untouched. | LIVE (r407) |
| F-3 | Structures (§7.13) | No structure base more than 0.1 m above terrain at any corner. NOTE: a race log showed `structure-hover` at 1.8 m on 2 structures while `test-nothing-floats` passed green — the gate has a blind spot for this class. | OPEN |

## G. Camera

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| G-1 | §6.4 | One camera per race; mode changes only in the pause menu. No automatic cuts. Probe raycasts terrain, canopy AND building colliders every tick including airborne. | LIVE |
| G-2 | §3.9 | Clamped closer than 6 m to the car, the camera RISES toward top-down instead of pulling in. | LIVE |
| G-3 | "There is camera shaking after the tunel" (2026-09-09) | The exit-side hand-off must be as steady as open road. r398 fixed the entry pair; its own note records the exit reaching +33 u over the crown, capped to ~+7 — a cap, not a cure. | OPEN |
| G-4 | C-CAM-01 (Race Integrity) | No frame majority-occluded by vegetation between camera and car; fade the intersecting instances. | OPEN |
| G-5 | C-CAM-02 | The camera collides with world geometry; backfaces never render full-screen. | OPEN |
| G-6 | C-CAM-03 | Airborne: car inside the central 60% of the screen; speedo shows ground speed, never 0; gear holds, never N. | OPEN |
| G-7 | FALKEN RIDGE frame (2026-09-09) | Tree crowns must not fill the frame at the camera. The canopy guard reads crown radius and top from `camTrees`, which the VERGE ring alone populates — the same registry gap as B-7(a). | OPEN |

## H. AI and difficulty

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| H-1 | §5 standing decision | DELETE THE RUBBER BAND. Removed in r313; `rubberBand`/`bandUp` are gone from EnemyCar. Convergence exists ONLY as the pressure rival's ±3% lease. Any future request to "bound rubber-banding" has nothing to bound — find the real cause. | LIVE |
| H-2 | "Opponents should match my car's strength. Needs to be a constant battle… Not me going away from them always" (2026-09-09) | `machineParity` matches grid pace to the player's machine class. | LIVE (r404) |
| H-3 | "Current hard level should be normal. Make 2 more harder levels where cars are more aggressive." (2026-09-09) | Ladder: EASY / NORMAL (= old HARD) / HARD / SAVAGE, ordered by `tier`. aiSpeed held flat at 1.06 across the top three because raising it INVERTS tier order via the `v > maxSpeed*0.55` nitro gate. Edge comes from aiCorner and aiAggression. | BUILT (r409) |
| H-4 | "Make it adaptive. If I'm driving good make them more angry" | Adaptation rides AGGRESSION ONLY, never pace — a live speed band is H-1's rubber band. Form is the player's progress lead over the best rival, smoothed 4 s, scaled by tier. EASY never gets angry. | BUILT (r409) |
| H-5 | §5.3 | FOLLOW → SETUP (≤1.5 s) → COMMIT (2.0 s) → CLEAR/YIELD. DEFECT FOUND in the owner's race log: SETUP steers to `aheadCar.lateral + side*3.5` while `aheadCar` requires `|across| ≤ 3.4` — 0.1 m outside its own detection window — so the rival loses sight of the car it is passing, drops to FOLLOW, and `_ovSetupHeld` resets, making COMMIT unreachable. Measured 18 flaps in 1.3 s; a pass never completes. | OPEN |
| H-6 | §5.4 | Token arbiter: ≤1 rival before GO+20 s, ≤2 after, rotate 6 s. OBSERVED: the same two cars re-acquire on every lapse — the cap holds but rotation does not. Top tiers raise the late cap to 3 (H-3). | LIVE / partly OPEN |
| H-7 | G-AI-01 (Race Integrity) | Bound rubber-banding both directions. NO MECHANISM EXISTS (H-1). The observed bunching and the 2nd→8th respawn swing need root-causing; H-5 is the prime suspect. | HELD — see L-5 |
| H-8 | measured r409 | THE PINCH CAP IS TIER-BLIND. `EnemyCar` caps corner speed through a narrow station at `(16 + 3.6*w) * pace * progRamp` — it carries the persona's pace and the roster ramp but not the difficulty. That was harmless while tiers were separated by aiSpeed, which the straights expressed; it is not now, because aiSpeed is flat at 1.06 across the top three (H-3) and the whole tier difference lives in aiCorner, which this cap discards. MEASURED: median rival 402 / 402 / 401 m on NORMAL, HARD and SAVAGE — three rungs, one pace. On FURKA RIDGE the three read 285 / 275 / 281 against a ±6 tolerance, so the suite's law 1 ("rival pace rises with difficulty") is now a coin flip there and goes red when the noise lands the wrong way. That red is r409's own, and it is left red rather than loosened: it names this defect exactly. The fix (carry sqrt(aiSpeed·aiCorner) normalised to NORMAL) was written and measured; it lifts HARD's field ~5% and takes HARD past the clean-winnable bound on FURKA RIDGE, so the tier table needs retuning in the same change. Held out of r409 rather than shipped half-done: the ladder's rungs differ in AGGRESSION, which is what the owner asked for, and in pace only on worlds without narrows. | OPEN |
| H-9 | measured r409 | EASY IS NOT CASUAL-WINNABLE BY THE TEST STAND-IN, AND HAS NOT BEEN FOR SOME TIME. The suite's law 5 (a 75% throttle drive within 2% of EASY's best rival) reads -5.3% on PINE VALLEY and -7.4% on FURKA RIDGE. PRE-EXISTING, not from this build: EASY's row (aiSpeed 0.74 / aiCorner 0.26 / aiAggression 0.65) is untouched by r409, and an A/B with the old 77 m grid restored reads -10.7% and -8.4% — worse, i.e. the r409 grid fix improved it. Prime suspect is r404 machine parity (H-2), which lifts the grid to the player's machine and only halves that on EASY (`parityCloseEasyMul` 0.5); a casual tier arguably should not chase at all. Not a deploy blocker — test-difficulty is outside the blocking gate's deploy set. | OPEN |

## I. Rendering, sky and readability

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| I-1 | "Shades needs to be consistent and constantly there" (2026-09-09) | One shadow law applied after every builder; wider, view-biased shadow box. | LIVE (r405) |
| I-2 | "Make blues skies and sun no fog" (2026-09-09) | Sky hem 0.14 (0.5 on dusk worlds), a real sun disc on the dome, ambient fog past everything reachable, haze-band curtains deleted, dome and stars follow the camera. | LIVE (r406) |
| I-3 | "Horizont should mit be as white" (2026-09-09) | The horizon band must not read white. Re-verify per theme on HEAD; where it still does, tint `skyHorizon` off pure white and/or narrow the hem — a per-palette decision, not a global. | OPEN |
| I-4 | "Also check for any unusually fog curtains" | Audit for any remaining curtain-class body (large, `fog:false`, inward-facing). | OPEN |
| I-5 | "There some wierd white horizontal lines" + "Remove the white triangle" + "Still see the lines" (2026-09-09) | ROOT-CAUSED r409: not scene geometry at all — the `#speed-lines` DOM overlay in index.html. Two `repeating-linear-gradient` families at 175° and 185° over `inset:0`, so each is a near-horizontal white stripe set and their 10° crossing reads as a long pointed sliver. A radial mask leaves the corners lit, which is the "white triangle". It is drawn OVER the HUD, which is why the sliver crossed translucent panels and no scene-child bisect ever found it. Fires from 95 km/h (`speedLinesFromKmh`), i.e. most of every race, while its CSS comment still said "past 150 km/h" — that stale comment is why five reports were chased into the world. Disabled by raising the threshold above reachable speed (see K-10). Earlier negatives stand and were not wasted: world-skirt 0 px, shadow acne, chairlift cables 0 px, rain, cloud-bank tails, haze bands — all ruled out, correctly. | FIXED (r409) |
| I-6 | §7.9 | Drop edges readable on every palette: the last 2 m before a drop uses the template's rock material. | LIVE |
| I-7 | P-PERF-01 (Race Integrity) | 60 fps sustained, no hitch above 33 ms. Measured 30-33 fps with 50-67 ms hitches. | OPEN |

## M. Economy, upgrades and career

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| M-1 | "Work on the game economy, upgrades, career building etc." (2026-09-10) | An open brief, not a defect report. Ground it in measurement before changing a price: `tests/test-economy.mjs` reads the constants where they are written and is GREEN on this base (upgrade steps 600/1100/2600/5100/8600, one line 18,000 CR, a strong race ~1,800, 110 races to max a car, 68 to own the roster). ECONOMY-PLAN.md's own standing advice is "not recommended: re-pricing anything" — the curve was measured into place at r148 and is defended by that test. So the work is where the plan says the gap is: REASONS, not money. | OPEN |
| M-7 | measured r410 | INCOME IS PAID FOR PLACING, NOT DRIVING. A staged 78-round career tallies race score at 84,916 CR of 253,318 — 33.5% — and the rest is flat: first clear 18.5%, podium 13.0%, championship points 11.9%, season prize 10.3%, finale double 7.4%, sponsor 2.8%, quest 2.5%. That 33.5% is FLATTERING, because the sim feeds a fixed race score of 4000; the live per-race census reads driving at 21-30% on most worlds and 2% on a finale (FALKEN RIDGE pays 2800 CR for a race that was 2% driving). M-2 lifts the driving share on 40 worlds; whether the flat side should also come down is a separate decision and is NOT taken here. | OPEN |
| M-8 | measured r410 | EARNING RATE IS A PROPERTY OF THE WORLD, NOT THE DRIVE. Same bot, shipped config, credits per minute: LANTERN QUARTER 67, EMBER PASS 102, RAZORBACK 110, CIDER LANE 119, REDWOOD 130, PINE VALLEY 152, COSTA BRAVA 200 — a 3x spread before finales, which pay 426. Score rate spans 0.76 to 4.96 points per second across worlds. So WHICH world you race moves income more than how well you drive it. Not yet root-caused: score comes from props, kills and style, and prop density is a world-build property nobody has levelled. | OPEN |
| M-9 | measured r410 | TERRAZZA ALTA scored 0 and paid 0 CR in 171 s of driving — the only world in the census that paid nothing at all. Either the centreline bot fails there or the world has no scorable content; unresolved, and quoted nowhere until it is. | OPEN |
| M-10 | measured r410, AFTER the threshold change | THE LAP FIX WORKED ON THE MIX AND BROKE THE RATE. Driving's share on worlds that flipped to 3 laps: PINE VALLEY 27% -> 86%, EMBER PASS 47% -> 79% — past even the 57% the A/B predicted, because a longer race also loses the CLEAN RUN bonus more often, which shrinks the flat half further. But credits per MINUTE fell with it: PINE 152 -> 50, EMBER 102 -> 91, against 128 on 1-lap REDWOOD and 180 on 1-lap FALKEN. The flat bonuses are paid once per RACE and a race is now 3x longer, so the 40 three-lap worlds are the WORST worlds to farm — a player optimising income would avoid exactly the worlds where driving matters most, which inverts the intent. PINE's per-race total even fell slightly (587 -> 552). CANDIDATE FIX, not taken without the owner: scale the per-race flat bonuses with lap count so a 3-lap race pays ~3x flat, making credits-per-minute roughly world-independent. That IS a re-pricing, which ECONOMY-PLAN.md advises against, so it is a decision and not a tidy-up. | OPEN |
| M-2 | §6.1c (r381), retuned r410 on the owner's pick | THE RULE HAD NEVER FIRED. `_lapsForTrack` races 3 laps under `lapsShortTrackU` and 1 over it, and the constant shipped at 3000 u — while a census of all 78 worlds reads min 4532 / p25 5783 / median 6935 / p75 8529 / max 12038. The shortest track in the game is 1.5x the threshold, so every world took the 1-lap branch and the 3-lap half of the r381 directive was dead code. MEASURED CONSEQUENCE, same world and bot with lap count the only variable (PINE VALLEY): 1 lap scores 883 in 227 s and pays 177 race CR against 430 flat — driving is 29% of the payout; 3 laps scores 2310 in 631 s and pays 462 against 350 — driving is 57%. `raceCr = raceScore × CREDIT_RATE × diffMult` scales with race length; PODIUM_CR, FIRST_CLEAR_CR and SWEEP_CR do not, so shortening the race silently halved what driving well is worth. Threshold now 7000 (owner's choice from the distribution, 40 of 78 worlds on 3 laps). No price changed. | BUILT (r410) |
| M-3 | ECONOMY-PLAN.md §2.1, shipped r161 | Contract rungs escalate per contract, persisted as `career.rungs = {contractId: rungIndex}`; absent means rung I. Bound: a full sweep of three rung-III contracts pays about one strong race, not three (asserted in tests/test-rungs.mjs). | LIVE |
| M-4 | ECONOMY-PLAN.md §2.2 | Rival signatures (blocker / late braker / opportunist) — behaviour, not stats, keyed to the driver. Bar: a blind test must identify which rival is which from the last third of a race, or the signatures are too subtle to have been worth the code. | OPEN |
| M-5 | ECONOMY-PLAN.md §2.3 | The daily line: one world per day, seeded from the date, fixed car and weather, one target time, NO credits — unpaid on purpose, so it never needs balancing against the campaign and never needs anti-farming rules. | OPEN |
| M-6 | ECONOMY-PLAN.md §3 | Acknowledgement, not content: name the moment (style events already detected), let one system react, and end the results card on the best thing that happened rather than a table. | OPEN |

## N. Track variants on existing worlds

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| N-1 | "Re use the tracks and redraw new racing scenes on the existing worlds. Make more jumps steep curves etc. Idea is to reuse the worlds for new tracks" (2026-09-10) | MOSTLY ALREADY BUILT — investigated before designing anything. A world is `{id, name, theme, region}` and the racing line is a SEPARATE named point list: `rawPts = CIRCUITS[level.route || level.theme]`, with `routeFlipX` and `routeReverse` modifiers already in place. 60 shapes serve 78 worlds. So a variant track is a new LEVELS entry carrying the SAME theme and region — all the terrain character, scenery and palette investment — with a different `route` naming a new hand-authored shape. No engine work is needed to make it possible; the work is authoring shapes and clearing §7. CAUTION 1: `seedForLevel` keys on `id` AND `route||theme`, so a variant gets its own seed and therefore its own terrain detail — same LOOK, not the same GROUND. Literal shared topography needs the seed keyed off the shared world instead (open question to the owner). CAUTION 2: the road conforms to terrain (§3.8) and HRD-5 builds flanks along the corridor, so a new line carves its own valley. CAUTION 3: "more jumps, steeper curves" is exactly where §7.5 min corner radius per template and §7.3 kicker landing zones bite — the validator is the constraint, not the authoring. | OPEN |

## J. Delivery

| ID | Rule | Status |
|---|---|---|
| J-1 | Every deploy bumps `build-tag` in index.html, the three `?v=rNNN` module URLs, and `CACHE` in sw.js. | LIVE |
| J-2 | `node tests/validation.mjs` MUST exit 0 before any deploy. A red blocks unless it is a recorded waiver with a ledger reference. | LIVE |
| J-3 | Every deployed build gets a narrative HANDOVER.md section above the previous one. | LIVE |
| J-4 | THE OWNER MAY NOT BE SEEING THE BUILD YOU SHIPPED. sw.js is cache-first for the navigation document, so a load renders from the old cache while the new worker precaches. Confirmed: the server served r406 while every phone frame read r405 across four loads. Fix: on activate + claim, tell the page and reload once at the menu. Do NOT make navigation network-first — with `ignoreSearch: true` that yields a fresh shell against stale modules. | OPEN |
| J-6 | Build practice, corrected 2026-09-10 | THE WORLD RNG IS PER-WORLD, NOT ONE GLOBAL STREAM. `worldSeed = seedForLevel(level)` (an FNV hash of epoch:id:route|theme) and `withSeed` swaps `Math.random` for the duration of that ONE world's Track construction, restoring it after. I had it recorded the other way — as a single stream where a changed draw count re-rolls every LATER WORLD — and acted on that reading more than once. What is actually true, and still binding: a draw added partway through a build re-rolls everything constructed AFTER it IN THAT WORLD, and since every world runs the same builder, that is a scatter reshuffle on all 78 at once. So new scatter is still written zero-draw (derive from an index already in hand, or place deterministically from the route) — but a NEW WORLD can be added without disturbing any existing one, which the old reading wrongly forbade. | LIVE |

## K. Recorded conflicts and their resolutions

Never resolve one of these silently. Each was found by reading, not by a bug report.

| # | Conflict | Resolution |
|---|---|---|
| K-1 | Elevation mandates (passes 1200 m, terraces 500 m) vs WR-2.1 grades 4-12% plus a 5.5 km lap. A lap climbing AND descending H at grade g needs length ≥ 2H/g, so 12% at 5.5 km caps the range near 330 m. | Master spec is newer and higher: ELEV_MANDATE compresses to the grade-lawful range per world. Recorded, not silently traded. |
| K-2 | R-RECOVER-01 trigger 2 (>15 m off spline → reset) vs C-1 and C-2. The patch's precedence clause defers to the MASTER SPEC, which does not cover these owner overrides, so the clause cannot settle it. | Triggers 1, 3, 4 shipped; trigger 2 HELD (L-1). The patch's own acceptance case (3-13 km/h) is met without it. |
| K-3 | R-RECOVER-01's 40 km/h rolling start vs C-3 ("a restarted car STANDS"). | HELD (L-2); C-3 kept meanwhile. |
| K-4 | G-AI-01 asks to bound a rubber band vs H-1, which deleted it in r313. | No band exists to bound. Root-cause the observed bunching instead; H-5 is the prime suspect. HELD (L-5). |
| K-5 | B-8 (trees cannot be pushed) vs §1's "smash and shove props with score". | The rule wins; tree-smash scoring goes. Flagged to the owner when shipping. |
| K-6 | W-EDGE-01a's 4 m band vs §7.11's deliberate 3 m verge setback. | The band as written measures the verge, not the fall, and misses both observed falls. Derive the band from measured sideways travel after a hit. OPEN (L-3). |
| K-7 | Three different drop thresholds: W-EDGE-01a says >2 m within 4 m; the shipped rail builder uses 2.5 m at a single 7.0 m probe; §7.11 says any step over 0.15 m at the road edge. | Unreconciled. Must be settled as one number before the audit is wired into the gate. |
| K-8 | The owner titled the Race Integrity patch RALLY_PATCH_02, but `RALLY_PATCH_02.md` already holds the 2026-09-06 document RALLY_MASTER_SPEC.md supersedes. | Filed as RALLY_RACE_INTEGRITY.md so neither is lost. Cite it as RALLY_PATCH_02 (Race Integrity, R21). |
| K-9 | Adding tiers above HARD vs three sites testing `difficulty.id === 'hard'` as shorthand for "top tier" (a contract gate, the rung ladder, a 1.25× payout). | Left alone, SAVAGE would pay like NORMAL and lock the hard rungs. All three compare `tier` now. |
| K-10 | Switching `#speed-lines` off vs r309, which lowered its threshold to 95 km/h for the owner sentence "do not feel I go 70". Both are owner directives; the later one ("Still see the lines", after four earlier reports of the same artefact) is about the same pixels. | The later sentence wins on the overlay. The r309 INTENT is not abandoned — the FOV push and camera pull already carry the sense of speed — but it will not be carried by white lines the owner keeps filing as a defect. `speedLinesFromKmh` set to 9999 (above any reachable speed) rather than deleting the element, so a future implement can re-enable it deliberately with a design that does not read as a sliver. |
| K-11 | "I don't see an order how do I win 1st place or 4th place" vs r363's REVERSE CHAMPIONSHIP GRID ("I should not be starting 1st always"), which deliberately puts the title leader at the back. | Not traded. The measured fault in the same sentence — 230 m of grid — is enough to explain an unreadable order on its own, and it is fixed (D-6). The reverse grid itself is left exactly as the owner asked for it in r363; whether it now reads as intended is a question for them (L-6), not a decision to take while fixing a units bug. |
| K-12 | "Add hazards and obstacles to Dodge and blow up across the tracks" (E-9) vs HRD-1 "RULE: nothing stands at the middle of the road", which is a HARD rule enforced by the blocking gate (test-nothing-on-road), and vs §7.3's 4 m exclusion band. Read literally, "obstacles across the tracks" means bodies in the carriageway, which is the thing the owner separately reported as a defect at r361. | Resolved by CLASS, not by exception: HRD-1 forbids a body that STANDS there. A hazard that ARRIVES (rockfall, rolling boulder, crossing traffic) or that the player DESTROYS is not a standing body, and neither is a destructible prop outside the 4 m band. No static obstacle is added to the racing line and no by-name gate waiver is taken. Flagged to the owner in case they did mean literal blockers, in which case HRD-1 needs amending FIRST and the gate suite with it.  OWNER ANSWERED "Yes" (2026-09-10) to the question "did you mean literal blockers in the carriageway", adding "Folow the graphics I send". So HRD-1 IS TO BE AMENDED — but not on my reading of one word. The amendment waits for the first hazard graphic (A-G), so that what lands in the carriageway is drawn against a reference the owner supplied rather than invented here, and so HRD-1 and test-nothing-on-road are changed once, precisely, with the class of body named. Until then no obstacle goes on the racing line and the gate suite is untouched. | RESOLVED 2026-09-10 BY THE FRAME. The owner sent the hazard graphic (annotated r409 CANYON RUN). Read as the brief it names the bodies (E-9) and settles the conflict narrowly: the only NEW class the frame puts inside the carriageway is a FLAT CHARGE PLATE lying on the dirt. CORRECTION, on measurement after this was first written: crates, cones and barrels are ALREADY on the drivable surface on every world — `_buildProps` puts them there deliberately as pass-through smashables and `test-nothing-on-road` counts rather than asserts them. So HRD-1 had an unwritten carve-out all along and the amendment is not opening a closed rule, it is writing the real one down and bounding it. The amendment is therefore ONE typed class with hard bounds (HRD-1a), not a hole in the rule and not a by-name waiver: <= 0.25 m tall, destructible, no velocity stop, telegraphed 60 m, one per 150 m, never in a gate clear zone, apex band, kicker landing zone or the finish run-out. `test-nothing-on-road` asserts those bounds rather than skipping the station. Everything else the gate forbade in the carriageway stays forbidden.
| K-13 | The E-9 hazard frame (2026-09-10) draws FLOATING RED TARGET / CROSSHAIR MARKERS hanging over the rock arches, marking what to shoot. Standing decision 2 says "no new on-screen elements, no world overlays", and it was written to erase exactly this family — the r297 yellow centreline and the rival arrows were floating world-space guides and the owner had them deleted with no replacement. | A-G says the frame outranks my inference, so I do NOT get to quietly drop it; standing decision 2 says "do not reopen", so I do not get to quietly build it either. HELD AND RAISED (L-7). The other five bodies in the frame are unambiguous and ship without it; the destructibles are readable on their own paint (red drums, stencilled crates) with no marker needed, so holding this one element costs the frame nothing. If the owner confirms the markers, standing decision 2 gets an explicit carve-out for diegetic target reticles on destructible bodies, recorded before any code. |
| K-14 | B-9 ("This should not be allowed", a car traversing a steep bank beside the road) requires the steep-face shed law to act INSIDE the 34 u rejoin band. The r328 fence deliberately exempts that band on the reasoning that "banks are moments, not faces", and it protects the rejoin: a car bouncing off a verge back onto the road must not be shed mid-bounce. | The owner frame outranks my inference (A-G), so the band stops being a blanket exemption — but the rejoin case it protects is real and was measured once, so the fence is NARROWED rather than deleted: inside the band the shed needs a grade clearly past the ceiling AND a dwell longer than a bounce, both numbers to be set from a measurement of the frame's bank and of a normal verge bounce, not chosen. If a narrowing cannot hold both, the rejoin case wins and the conflict comes back to the owner. Nothing built until the angle is measured. |
| K-15 | The r410 gate turns green on GLACIER COL only because a WAIVER absorbs the failure, and the waiver does not fit it. The line is `F7 GLACIER COL: grass tops 66 vs 190 km/h = 35%, runway 5@12, NO brush-free corridor (best 1 brushed samples)`. The waiver (tests/validation.mjs:69) matches on `/(PINE VALLEY|GLACIER COL): grass tops/` — A WORLD NAME AND NOTHING ELSE — and its stated cause is "thrust-equilibrium marginal vs [55,75]" with its own ledger recording GLACIER COL at 89% on the r403 base. 35% is not marginal, it is half the bound, and "NO brush-free corridor" is a different failure mode from an equilibrium read. The prime suspect is r410 itself: registering the mid forest ring put colliders and brush where the runway search now cannot find a clean line. | TWO SEPARATE FAULTS, BOTH RECORDED, NEITHER SHIPPED SILENTLY. (a) THE GATE: a name-matched waiver cannot tell the failure it was written for from a new one, so it will hide any future F7 regression on those two worlds. It needs a MAGNITUDE BOUND in the match (waive only within the band the ledger actually measured) so a real regression cannot shelter behind it. (b) THE BUILD: r410 must be A/B'd with the mid-ring registration on and off, on GLACIER COL, before r410 is called good — if registration is the cause then r410 traded a forest-containment fix for an off-road surface regression and the owner is owed that trade, not a green tick. Neither is touched while the gate is running against this tree. |

## L. Open questions for the owner

| # | Question |
|---|---|
| L-1 | R-RECOVER-01 trigger 2: reinstate the unconditional ">15 m off the spline for 2 s" reset, reversing C-1 and C-2? Shipped without it; the patch's own acceptance is already met. |
| L-2 | R-RECOVER-01: 40 km/h rolling start, or keep C-3's standing restart? Kept C-3. |
| L-3 | W-EDGE-01a: set the lateral band yourself, or shall I derive it from measured sideways travel after a 250 km/h hit at the two known spots? |
| L-4 | E-3: the elevation/weave build is reverted and held pending containment fixes. Re-land when? |
| L-5 | G-AI-01: accept root-causing the bunching (H-5) instead of adding a band to bound? |
| L-6 | The grid is now a real grid (10 m rows, 29 m deep) but still REVERSED by season standings, so leading the championship means starting 8th — your r363 request. With the spacing fixed, does the reverse order stay, or should the grid form the normal way round? |
| L-7 | The hazard frame draws floating red target markers over the arches. Standing decision 2 deleted exactly that family (the r297 centreline and rival arrows) and says do not reopen. Do you want the markers, or are the red crates and drums marker enough? Everything else in the frame is being built either way. |

---

## Appendix. Where the other documents sit

| Document | Holds |
|---|---|
| RALLY_MASTER_SPEC.md | HUD H-01..03, track T-01..05, containment W-EDGE-01 + W-CURVE-01, scenery S-01..05, vegetation V-01..02, validation VAL-01..03, change control C-01..02. Above CLAUDE.md in authority. |
| RALLY_RACE_INTEGRITY.md | The R21 patch: R-FINISH-01, W-EDGE-01a/02, R-RECOVER-01, C-CAM-01/02/03, G-DMG-01, G-WRECK-01, W-CURVE-02, G-AI-01, W-TEX-01, P-PERF-01. |
| RALLY_W_CURVE_01.md | Standalone copy of the curve containment rule. |
| RALLY_PATCH_02.md | The 2026-09-06 document, superseded entirely by the master spec. Kept for history. |
| CLAUDE.md | Working spec: build order, module map, acceptance queries, §7A HRD table, the recording ledger. |
| RULES.md | World rules and object reference: collision classes and their exact mechanics. |
| DRIVING_SPEC.md / RALLY_DRIVING.md | Vehicle physics constants. |

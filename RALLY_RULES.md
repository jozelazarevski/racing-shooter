# RALLY_RULES.md — the owner's rules, all of them, in one place

**Authority.** CLAUDE.md names this file the top of the chain:
`RALLY_RULES.md (race outcomes) > RALLY_DRIVING.md (vehicle physics constants)
> CLAUDE.md > code`. Until 2026-09-09 the file did not exist, so the top of
the chain was a dangling reference and every rule had to be re-derived from
CLAUDE.md prose, HANDOVER narrative, or the code. That is the defect this
file closes.

**What is in here.** Every OWNER DIRECTIVE, verbatim where the words are on
record, with the operational rule it became and its current status. Specs
that are documents in their own right (RALLY_MASTER_SPEC.md,
RALLY_RACE_INTEGRITY.md, RALLY_W_CURVE_01.md) are cited, not copied — but
every rule they contain that CONFLICTS with something else is recorded in
§K, which is the section to read before implementing anything.

**How to use it.**
1. Cite rules by ID (`R-3.2`, `HRD-7`, `AI-2`). IDs never change meaning; a
   superseded rule keeps its ID and gains a SUPERSEDED-BY line.
2. Before writing code that touches a rule, check §K for a conflict on it.
3. A new owner sentence goes in here FIRST, with a status of OPEN, before
   any code is written against it.
4. Never resolve a conflict silently. Record it in §K with the resolution
   and the reason, or leave it in §L for the owner.

**Status vocabulary.** LIVE (shipped and deployed) · BUILT (in the tree,
not yet deployed) · OPEN (agreed, not yet built) · HELD (blocked on an
owner decision, see §L) · SUPERSEDED.

---

## A. Standing decisions — do not reopen

| ID | Rule | Status |
|---|---|---|
| A-1 | The world is fully drivable. No invisible walls, no rule-based speed caps, no return forces. Containment is physics (slope, surface, gravity, terrain collision) and race structure (gates). | LIVE |
| A-2 | Repair only for UI: no new on-screen elements, no world overlays. The r297 yellow centreline and rival arrows are erased with no replacement. Scope LIFTED for world and track geometry by RALLY_PATCH_02 v3 C-4. | LIVE |
| A-3 | The in-race HUD is frozen exactly as shipped in recording E. Nothing moves, resizes, restyles or is removed. Toast BEHAVIOUR may change; toast APPEARANCE may not. | LIVE |
| A-4 | No minimaps, ever, in any corner or mode (RULES.md §0.1; removed twice). Any request that seems to imply a map is re-checked against this first. | LIVE |
| A-5 | No route lines, arrows or braking markers. Corner readability comes from world design: scenery framing, tree lines tightening into curves, containment as natural signposting. | LIVE |
| A-G | "Folow the graphics I send" (2026-09-10) | AN OWNER FRAME IS THE BRIEF, NOT AN ILLUSTRATION. Where the owner sends a picture with a request, that image is the target and the spec: match what it shows rather than what the words could also mean. This is already how r395 (the mountain-flank reference frame) and r393 (the tree design sheet) were built, and it is why the white-line hunt was worth five rounds — the frames were right and the reading of them was wrong. A frame also OUTRANKS my inference: if a picture and a rule disagree, raise the conflict in §K, do not quietly follow the rule. | LIVE |

## B. Physics and containment

| ID | Owner sentence / source | Rule | Status |
|---|---|---|---|
| B-1 | CLAUDE.md §3.1 | Terrain is a heightfield; overhangs/tunnels/arches are separate closed trimeshes. Validator rule. | LIVE |
| B-2 | §3.3 | Above `maxClimbDeg` 35° a wheel produces no drive and lateral grip decays to 0 over 0.5 s — the car slides down rather than hanging. | LIVE |
| B-3 | "Should be wrecked at this kind of falls" (r320, PIKES PEAK shelf) | Falls past the damper-priced free band cost hull. | SUPERSEDED-BY B-4 |
| B-4 | RALLY_PATCH_02 v3 FIX-7 | A cliff fall costs −1 hull retained, respawns on the road unfrozen with 3 s invulnerability, camera stays on the player. DESTROYED by terrain alone MUST be unreachable in a normal run. | LIVE |
| B-5 | "Rule: when I get out of the cliff I need to drop immediately" (2026-09-09) | Leaving a lip hands the car to gravity on the same frame. MEASURED CAUSE: `fallEdgeDrop` is 3 m, so the car rappels the first 3 m at up to 11 u/s before going ballistic. The code's own note gives the principled value — below ~0.9 u the ground-follow ease is already inside its cap — so the threshold is ~1.0. | OPEN |
| B-6 | "Car should not drive between the trees" (§7.15) | A forest stand is a MASS: trunks stop a car like any solid, and dense stands carry heavy underbrush drag. Physics, never an invisible wall. | LIVE |
| B-7 | "Rule: I can't drive through a tree" (2026-09-09), RE-REPORTED "Should not be able to drive in the trees - rule" (2026-09-10) — the third owner sentence on this, after r399's "Car should not drive between the trees". Three sentences means the r399 build did not close it and this is now a build, not a backlog row. | Every tree the car can reach is solid. THREE MEASURED GAPS: (a) the forest carpet registers colliders for the VERGE ring only. LOCATED r410: `_buildForestCarpet` calls `ring(...)` four times and only the verge call passes its trailing `reg = true`, so `camTrees` holds the 9,000 verge instances of `trackSpot(1, 38)` and nothing else. The mid ring is `trackSpot(38, 160)` at 14,000 instances with `reg` omitted — an uncollided band 38 to 160 u out from the road edge, on every carpet world, which is exactly the belt a car reaches when it leaves the road. The horizon ring is `radialSpot(170, maxR+450)` at 24,000. BOTH the trunk colliders and the 7.15 underbrush drag read `camTreesNear`, so in that band there is neither a trunk to hit nor brush to bog in: the r399 build made the VERGE a mass and left the forest behind it paint, which is why the owner can still drive in the trees; (b) a trunk hit at ≥16.7 m/s keeps 60% of approach speed (FIX-5, r388) on BOTH collision paths — which contradicts B-6, B-8, and §7.15's own shipped words ("no 60%-and-through deflection"). The rule text and the code disagree today; (c) `this.trees` carries `solid:false` on saplings, snags, palms, vines, cacti and small oaks. | OPEN | RE-REPORTED A THIRD TIME: "I can still drive in trees" (2026-09-11, after r399 AND r410). ROOT CAUSE FOUND, AND IT IS NOT A SECOND CODE PATH — IT IS THE GATE IN FRONT OF THE PATH BOTH FIXES EDITED. The carpet-trunk collision in vehicles.js begins `if (this.pos.y > tr.top + 1 || this.pos.y < tr.top - 11) continue;`. The lower bound is measured from the tree's TOP, so a car standing on the ground beside a tree whose crown is more than ~11.5 u above it fails the test and the trunk is skipped BEFORE any collision response runs. r399 and r410 both fixed the RESPONSE (deleting the 60%-and-through deflect on each path); neither touched the condition deciding whether the response executes at all. MEASURED, trees within 40 u of the road: PINE VALLEY 2523 of 3469 skipped (72.7%), REDWOOD RAMPAGE 2402 of 3538 (67.9%), DEEPWOOD TRAIL 2416 of 3376 (71.6%), GLACIER COL 2079 of 3768 (55.2%); median tree height 12-14 u against an 11 u cut-off. Most of the wood was never solid. THE LESSON, recorded because it generalises: TWO FIXES THAT BOTH "WORKED" AND BOTH FAILED MEANT THE TESTED CODE WAS NEVER REACHED — the same shape as the wrap-count bug, where byte-identical output was the tell of a fix that never ran. Fix: gate on the tree's BASE (`tr.y`, present on 100% of entries) rather than its top. FIX LANDED AND VERIFIED ON GEOMETRY (r411 pending gate): the band is now measured from the tree's BASE. Before/after on trees within 40 u of the road — PINE VALLEY 2523 skipped -> 0, REDWOOD RAMPAGE 2402 -> 0, DEEPWOOD TRAIL 2416 -> 0, GLACIER COL 2079 -> 25 (0.7%, and those 25 are the malformed entries whose crown reads below their own base, correctly rejected by the top test). Both intended skips confirmed still rejecting on PINE VALLEY: with the car on the ground all 3469 trees collide; with the car above the canopy, 0; with the car below a ledge tree, 0. So the wood became solid without becoming a ceiling or a floor. ONE WORRY RAISED AND REFUTED: admitting ~3x more trees could have reached a malformed entry and written a bad position into the car — measured, PINE VALLEY has ZERO non-finite entries (x/z/r/top/y all finite), so that risk was not real. The drive-into-a-stand confirmation is still outstanding, and the one-tree-per-frame `break` in the trunk loop is still unmeasured — with three times the trees live a car may yet squeeze between two, and 7.15's brush drag is the backstop. Neither is changed speculatively.
| B-8 | "Rule: I can't push trees" (2026-09-09) | No tree yields, is felled, or moves. Deletes the smash/shove class FOR TREES. CONSEQUENCE TO ACCEPT: tree-smash scoring goes with it (§1 "smash and shove props with score"). Vines (`kind: 'vine'`, hanging foliage, not a trunk) stay non-solid or the jungle gains mid-air walls. | OPEN |
| B-9 | "This should not be allowed." (2026-09-10, phone frame, r409 BRIDGE RUN) | A CAR MAY NOT DRIVE ALONG A STEEP BANK. The frame shows the car OFF the carriageway on the grass embankment beside a guard-railed road, nose down the fall line, holding 31 km/h and TRAVERSING the slope rather than sliding off it — a rival truck is doing the same lower down, so this is not a player-only quirk. §3.3 already says that above `maxClimbDeg` (35°) a wheel makes no drive and its lateral grip decays to zero over 0.5 s "so the car slides down instead of hanging". That law is written for CLIMBING; this is CROSS-SLOPE, and the frame is the evidence it does not bite there. THE RULE: on ground steeper than `maxClimbDeg`, a car loses lateral hold in every direction, not only uphill — it slides down the fall line and cannot hold a line across the face. Off-road is still never a fault (C-1, C-2): the car slides, it is not reset. NOT YET DIAGNOSED — measure the actual slope angle under that car before touching the law, because if the bank is under 35° the fault is the terrain, not the physics. | OPEN | STATIC READ OF THE PHYSICS, 2026-09-10 (a hypothesis, NOT yet measured — the bank angle in the frame is still unmeasured). The "face sheds the car" law exists and is correct (vehicles.js ~2103, r330): past `maxClimbTan` gravity pushes the BODY downhill, and it deliberately triggers on the TRUE gradient magnitude, not the heading-projected grade, precisely so "a car parked sideways across a 60 deg face" is caught — which is this frame's geometry. IT DOES NOT FIRE HERE BECAUSE OF A FENCE, NOT A GAP IN THE MATHS: the whole block is gated on `farOffRoad`, which needs `|lateral| > rejoinBandU` (34 u) or 34 u from the tracked centre. The r328 fence was added on the reasoning that within that band "banks are moments, not faces". The owner's frame is a car on a bank INSIDE that band, and says it should not be allowed — so the fence, not the law, is the defect. THIS IS A CONFLICT WITH A DELIBERATE PRIOR DECISION and is filed as K-14, not silently traded: the r328 fence protects the rejoin (a car bouncing off a verge to get back on the road must not be shed mid-bounce), so the fix is to NARROW the fence, not delete it — e.g. shed inside the band too once the grade is well past the ceiling AND the car has held the face longer than a bounce. Also note `slopeAuth` already zeroes drive above 35 deg, so a car crossing at 31 km/h inside the band is COASTING on momentum with grip it should not have; the lateral-decay half of §3.3 is the other thing to check once the angle is measured.
| B-10 | Consolidation, 2026-09-10 — three owner directives, one family | B-5 ("when I get out of the cliff I need to drop immediately"), B-9 ("This should not be allowed", a car traversing a steep bank) and E-8 ("This as well", an unguarded cliff lip at 199 km/h) are THREE REPORTS OF THE SAME SUBJECT — what the game believes about an edge and a face — and each has now been root-caused to a DIFFERENT mechanism, so they do NOT collapse into one fix: B-5 is a THRESHOLD (`fallEdgeDrop` 3 m, should be ~1.0); B-9 is a FENCE (the steep-face shed law is gated to >34 u from the road by the r328 rejoin exemption, and the owner's bank is inside it); E-8 is a BLIND PROBE (one terrain sample 7 u out decides whether a fall exists, in the rail builder AND in W-CURVE-01.4, plus that check only examines corners of radius <= 30). What they DO share is why the gate never caught any of them: every one is a case the instrument was not asked about. SEQUENCING, so they are not three half-builds: B-5 is a one-number change and can ship alone; B-9 and E-8 both change how much of the world is guarded or shed and will move many worlds at once, so each needs its measurement first (B-9: the frame's bank angle and a normal verge bounce; E-8: how far out the lip actually sits) and each is its own build under the one-system-per-build rule. | OPEN |

## C. Recovery and resets

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| C-1 | "Don't reset the car when I go off route" (r345, §3.6c) | The player's missed-gate return is DELETED. Leaving the route costs progress, never the car. The owed gate stays armed; driving back through it clears the debt. Rivals keep their off-course recovery — a parked rival is a bug. | LIVE |
| C-2 | "Don't reset me when I am off-road" (r364, §3.6d) | The cliff-top auto-return is DELETED. Being off-road, however high, is never a fault. Only physical traps rescue: under-terrain/void, wedged under held throttle, bogged on wrong tyres, upside down, fatal falls, water. | LIVE |
| C-3 | "When car restarts after selecting goes straight to nitro. Should be stopped." (r324, §3.6b) | A restarted car STANDS: `returnSpeedKmh` 0, and every placement zeroes a burning boost. Heading-on-tangent still applies. | LIVE |
| C-4 | R-RECOVER-01 (Race Integrity) | Off-road, held throttle, under 25 km/h and under 8 m of along-track progress in the window is STUCK and is rescued. On the road the 1 m bar stands. Gated on held throttle and on being off-road, which is what keeps C-1 and C-2 intact. | BUILT (r408) |
| C-5 | R-RECOVER-01 trigger 2 | Off-road AND >15 m from the spline for 2 s → auto-reset. | HELD — see L-1 |
| C-6 | R-RECOVER-01 reset behaviour | 40 km/h rolling start. | HELD — see L-2 |

## D. Race outcomes

| ID | Rule | Status |
|---|---|---|
| D-1 | Lap counter starts at 1. Start-line trigger inert until checkpoint 1 is passed; no event, no message. (§6.1) | LIVE |
| D-2 | "I don't need 3 laps. Race is one lap only." (r364) — default lap count 1; a world's own `laps` still wins. | LIVE |
| D-3 | "Decide when track is 1 or 3 laps depending the length." (r381) — under `lapsShortTrackU` (3000 u) races 3 laps, longer races 1. | LIVE |
| D-4 | R-FINISH-01: crossing the finish MUST end the race within 500 ms, in every vehicle state. MEASURED CAUSE: there is no trigger volume — the finish is an index wrap gated on a checkpoint mask, and r311's gate-0 guard seated a return ON the line, so the teleport CONSUMED the crossing. Fixed by seating before the line (carrying lap and wrap counts back with it) and by giving every placement two frames of immunity from both wrap tests. | BUILT (r408) |
| D-5 | Kills affect position: a destroyed rival respawns at its last gate after a 4.0 s hold. (§6.6) | LIVE |
| D-6 | "The cars start really strange. I don't see an order how do I win 1st place or 4th place. Also they all start super far away. Unusual for a race." (2026-09-09) — THE GRID IS MEASURED IN METRES. `gridSlot` spaced rows by `row * 8` centreline SAMPLES, written when a sample was a couple of metres; since the r340 length doubling `segLen` is 6.1-13.4 u across the roster, so eight samples is 53-77 m a row. FALKEN RIDGE measured 77 m per row, 230 m pole-to-back, pole itself 96 m up the road from the line. Now `gridRowGapM` 9 / `gridPoleBackM` 8 / `gridLateralU` 3.6 in driving.json, converted through each world's own segLen (rounded, floor one sample so two cars sharing a lateral can never share a station). Measured after, ten worlds: rows 6.1-13.4 m, depth 18-40 m, eight distinct slots everywhere (FALKEN RIDGE 10 m rows, 29 m depth). | BUILT (r409) |

## E. World and track

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| E-1 | "All Tunels needs to be under a mountain otherwise makes no sense." (r350) | Every bore lies under real terrain mass end to end, flank overburden ≥25 m. | LIVE |
| E-2 | "Olive coast make it like small hills next to the sea." (§7.16) | On a coast world the water MUST be visible from the coast road; the band between road and waterline is rolling knolls. Acceptance is the sightline census, not the eye. | LIVE |
| E-3 | "Lower the height difference across the game. Instead of the straight climb introduce more interesting snake turns going up and down." (§7.17) | The ROAD's height range per lap is modest; a sustained climb is SNAKED and undulating, never a straight ramp. | OPEN (build reverted, see L-4) |
| E-4 | "Add more curves and jumps" (2026-09-09) | Raise corner density and kicker count roster-wide within §8 template bounds, W-CURVE-01 and T-04. | OPEN |
| E-5 | W-CURVE-02 (Race Integrity) | No segment may hold the car at top speed with zero steering for more than 12 s. Observed 45 s at 250 km/h. | OPEN |
| E-6 | Speed budget (§6.3, §7.5) | `nitroCeilingKmh = min(designSpeedKmh + 20, gearTop + 40)`. NOTE: 250 km/h was observed on an alpine world whose design speed is 160, so the ceiling should be ~180 — a breach the Race Integrity patch does not itself flag, and one that makes E-5 worse than it looks. | OPEN |
| E-7 | W-EDGE-01a (Race Integrity) | A drop beside the road needs containment along its whole length. MEASURED PROBLEM: at the patch's own numbers (>2 m within 4 m of the edge) FALKEN RIDGE — where both observed falls happened — has ONE violating station in 900, because §7.11 deliberately sets drops back beyond 3 m of verge. Band sweep on that world: 4 m → 1 station; 8 m → 299, longest run 1005 m; 15 m → 460, longest run 1771 m. The band must be derived from how far a car actually travels sideways after a hit at racing speed, not picked. | OPEN — see L-3 |
| E-8 | "This as well" (2026-09-10, second phone frame, r409 BRIDGE RUN) | AN UNGUARDED CLIFF LIP ALONGSIDE A FLAT-OUT SURFACE. The frame shows the car at 199 KM/H on the broad tan shoulder, with a sheer cliff falling away immediately to the left. Containment along that lip is ONE TRAFFIC CONE. The previous frame from the same world had guard rails; this stretch has none. Three rules already cover pieces of this and none of them fired here: §7.11 says the drop side MUST NOT be reachable at speed from the racing line without crossing at least 3 m of verge; §7.9 says the last 2 m before a drop uses the rock material, not grass, and the lip in this frame is unmarked ground; C-3 as amended permits BREAKABLE guardrails on any drop over 15 m. THE RULE: where the drivable surface runs beside a drop, it carries containment — breakable rail, or a verge wide enough that the lip is not reachable at speed — AND the last 2 m reads as an edge. A cone is not containment. THIS FRAME SETTLES K-7: three different drop thresholds are on the books (W-EDGE-01a >2 m within 4 m, the shipped rail builder 2.5 m at a single 7.0 m probe, §7.11 any step over 0.15 m) and the reason this lip has nothing is almost certainly that the rail builder's single probe missed it. Measure the drop and the probe before writing the fix. | OPEN | STATIC READ OF THE BUILDER, 2026-09-10 (a hypothesis, NOT yet measured on the world in the frame). `_buildEdgeRails` (track.js:13689) decides "is there a fall here" from ONE SAMPLE at ONE fixed lateral: `out = pointAt(i, (half + 7.0) * side); drop = center[i].y - terrainHeight(out.x, out.z)`, rail only if `drop >= 2.5`. A single point 7 u out cannot see a lip whose fall begins FURTHER out than 7 u — on a world with a broad shoulder (and the frame shows exactly that, a wide tan shoulder with the cliff beyond it) the probe lands on level verge, reads ~0, and the whole stretch is dropped before the tight-corner rule or the run detector ever look at it. A shelf at 7 u bracketing a notch nearer in fails the same way. Candidate fix shape: sample a RANGE of laterals across the reachable verge (roughly half+2 to half+14) and take the MAX drop, so any lip inside the belt a car can reach arms the rail. MUST BE MEASURED FIRST on the world in the frame — how far out the lip actually sits — and it will add rails roster-wide, so MAXBAY (420) and the `barriers` per-frame walk are both in scope. This also settles K-7 once measured. AND THE SUITE SHARES THE BLIND SPOT, WHICH IS WHY THE GATE IS GREEN ANYWAY. W-CURVE-01.4 in `test-stagerules.mjs` (~line 289) asks "do sharp drop curves carry a barrier within 8 u" using THE IDENTICAL SINGLE-POINT PROBE — `out = pointAt(i, (half + 7.0) * s)`, skip the station if the drop there is under 2.5 u. So the check that is meant to catch an unguarded lip cannot see any lip the BUILDER cannot see: both ask the same question at the same one point, and a station whose fall starts beyond 7 u is silently dropped from the census rather than failed. A SECOND filter narrows it further: the loop begins `if (radE(i) > 30) continue`, so only SHARP CURVES are examined at all — a straight or gentle bend beside a cliff is never checked, and the frame shows 199 km/h, which is not a sharp curve. CONSEQUENCE, and it is the point: a green W-CURVE-01.4 is NOT evidence that drops are guarded, and no past green should be cited as if it were. The fix must move BUILDER AND SUITE TOGETHER (a belt-sampled max drop in both, and the curvature filter widened or dropped); fixing only the suite would turn it red roster-wide, which would be CORRECT but is a large separate piece of work that must be sized before it is started.
| E-9 | "Add hazards and obstacles to Dodge and blow up across the tracks" (2026-09-10) + "Yes. Folow the graphics I send" + THE HAZARD FRAME (2026-09-10, an annotated r409 CANYON RUN capture) | THE FRAME IS THE BRIEF (A-G). What it shows, read as the spec, is SIX bodies and one effect: (1) EXPLOSIVE / DANGER CRATES — red-painted wooden crates with a stencilled hazard legend, singly and in stacks of two to three, standing on the verge and on rock ledges beside the road; (2) FUEL / GASOLIN BARRELS — red drums in clusters of three to five, verge and ledge, the same class as the crates; (3) DYNAMITE BUNDLES strapped to the cacti — the existing scenery body carries the charge, no new mesh; (4) CHARGE PLATES laid FLAT ACROSS THE DIRT — the frame puts these squarely in the carriageway; they are new, and HRD-1a bounds them (the crates and drums do NOT need that amendment, since the pass-through prop class is already on the surface roster-wide); (5) FLOATING RED TARGET MARKERS over the rock arches — HELD, raised as K-13 against standing decision 2, not built with the rest; (6) EXPLOSION BURSTS — VFX on destruction, the existing debris path. Classes: 1-3 are DESTRUCTIBLE, off the driving surface, priced as smash/shove and scored; 4 is the new low-profile class bounded by HRD-1a; plus the DODGEABLE arrivals E-9 already carried (rockfall, rolling boulders, crossing traffic) which stand in nobody's way until they arrive. Density and placement otherwise follow §7.3 unchanged, and kicker landing zones stay clear. | SPEC'D, NOT BUILT |
| E-10 | "Add wide fields scenarios. Those where there and got deleted" (2026-09-11) | WIDE OPEN FIELD RACING, restored. RECORDED OPEN because the search for what was deleted came back empty and the reading changes the build completely. WHAT I CHECKED: (a) NO WORLD WAS DELETED — 78 worlds now, 78 before, and §9 rule 5 forbids changing the count; (b) HRD-5 (r395, "always a mountain range on one side") was the obvious suspect for walling in open horizons, and it is NOT: it returns early unless the world carries the MOUNTAIN scenery tag, and the field themes carry FARMLAND / PLAINS / DESERT instead — the owner's own scoping ("there can be field roads. That is fine") is honoured in code; (c) the field builders are still present (hedge banks, stubble and orchard palettes, HARVESTVALE crop country). WHAT I DID FIND, and it fits the complaint better than any deletion: CLAUDE.md §8 specifies an "open" TEMPLATE — the widest and fastest in the game, halfWidth 8 m, designSpeedKmh 200, min corner radius 60 m, gate mix 0/30/70, boundary NONE, no pedestrians — and NO WORLD IMPLEMENTS IT. There is no template system in the code at all: worlds are hand-authored per theme, and only ONE world carries PLAINS (SAFARI PLAINS, id 26). So the wide-open class exists on paper and not in the game. Whether that is what the owner remembers, or a scenery look, or free-roam meadow touring (r292), is L-8.  OWNER ANSWERED 2026-09-11: FAST OPEN-FIELD TRACKS — build to the "open" template CLAUDE.md §8 already specifies and nothing implements (halfWidth 8 m, designSpeedKmh 200, min corner radius 60 m, gate mix 0/30/70, finish run-out 120 m, gate spacing <= 300 m, off-surface sand/grass, boundary NONE, pedestrians 0, kickers 1-3 per lap, no readability pass) — long sweeping curves through crop country, no walls, the opposite of the mountain shelf roads. THE CONSTRAINT THIS HITS, recorded before any code: §9 rule 5 says world count, chapters and star ratings are unchanged, so this is a CONVERSION of existing worlds, not an addition of new ones. The C-4 scope amendment permits exactly that — track geometry, length, curvature and scenery bands may be redesigned. Candidates are the worlds that are already field country and are currently built to no template at all: SAFARI PLAINS (id 26, the only PLAINS world), the three farmland worlds, two harvestvale, two vineyard, two outback. A conversion is a per-world redesign under one-system-per-build, so it is piloted on ONE world, measured against the template numbers, and only then swept. Blocked behind r411 (the tree-collision gate fix) shipping. | ANSWERED, NOT BUILT | MEASURED BEFORE BUILDING, AND IT MOVES THE TARGET. Two things the spec numbers hide: (1) UNITS — CLAUDE.md §8 gives the open template in METRES (halfWidth 8) but the game is in `u` and `ROAD_HALF` is already 9, so pasting the spec numbers would NARROW the roads. The template is a SHAPE, not a set of constants to copy. (2) THE FIELD WORLDS ARE ALREADY WIDE AND ALREADY FAST — what they lack is CURVATURE. SAFARI PLAINS (id 26) measures: lap 7082 u, half-width 9 u throughout, corner radius p05 197 / p50 810, only 0.3% of stations under R45 — and 95% OF THE LAP IS STRAIGHT (radius over 200 u). That is not an open track, it is a straight line with fields beside it. So the deliverable is NOT width and NOT speed: it is LONG SWEEPING CURVES, the template's actual character ("min corner radius 60" is a FLOOR for sweeps, not a target for straights). The work is to replace straight-line running with fast open bends in the R60-200 band, keeping the 9 u width and the high design speed. This also merges with two items already queued: T-01's curvature programme (pilot on the zero-curve worlds) and the owner's roster-wide "Add more curves and jumps" — SAFARI PLAINS at 95% straight IS a zero-curve world, so E-10 is the pilot for all three.
| E-11 | "Most tracks look the same like this." (2026-09-11, frame r412 GRANITE NARROWS) | THE ROSTER READS AS ONE WORLD. The frame: pale gravel road running between dense mid-green conifers on both sides, a pale grey-green mountain behind, plain blue sky. Nothing in it says WHICH of the 78 worlds it is. The complaint is about VARIETY, not about this world being wrong — so the fix is not to restyle GRANITE NARROWS, it is to find what is shared across the roster and make worlds differ from each other. MEASURE FIRST, and measure the right thing: what reaches the eye in a driving frame is (a) the road surface colour, (b) the verge and ground-cover colour, (c) the tree SPECIES MIX and canopy colour, (d) the horizon mass and its colour, (e) the sky and light. A census of those five across all 78 worlds says how many distinct LOOKS the game actually has, as against how many themes it declares. Related but NOT the same as E-10 (which is track SHAPE — curvature); this is the PICTURE. | OPEN | MEASURED 2026-09-11 (tools-scratch/dbg-lookcensus.mjs, a static parse of THEMES + FLORA_MIX — no browser, since building 78 worlds costs the forty minutes the gate takes and answers the same question). Each world reduced to a LOOK KEY: sky top, sky horizon, fog, hemisphere ground, dominant tree species, colours bucketed coarsely so the question is "could a driver tell these apart", not "do the hex digits differ". VERDICT: 73 worlds parsed, 41 declared themes, 33 distinct looks — and TWENTY OF THE 33 ARE SHARED BY TWO OR MORE WORLDS. The exact-match clusters: SIX worlds on one med/olive look (OLIVE COAST, AUTODROMO VELOCE, TOUR DES CAPS, CAPE OLIVETO, TERRAZZA ALTA, SALINE SPRINT); FOUR on one pine look (PINE VALLEY, BIRCHLAND CRESTS, ARDENNES SWEEP, WALDSCHLEIFE); three alpine-pine (SUMMIT CLIMB, HIGHCROWN PEAK, ALPENRING); three farm-oak (HEDGEROW DASH, AERODROME CIRCUIT, ORCHARD PARK); two snow (GLACIAL PASS, AVALANCHE ALLEY). Same sky, same fog, same ground tint, same dominant tree — the owner is right and it is not impressionistic. TWO LIMITS OF THE INSTRUMENT, recorded so the number is not overread: it parses 73 of 78 worlds, and the 12 harbour-family worlds resolve to an EMPTY key because their themes are generated (`THEMES[key] = {...THEMES.harbor, ...over}`) rather than written as literals — so the apparent 7-world cluster of "-|-|-|-|-" is a PARSE ARTEFACT, not evidence, and the true look count is 33 plus whatever those 12 resolve to. WHERE THE FIX GOES: not a restyle of any one world. The lever is that a THEME is shared by many worlds and carries the entire palette, so worlds on the same theme are identical by construction. Variety has to come from PER-WORLD variation within a theme — the `tune` block already exists for exactly this and is used by only a handful of worlds. | OPEN, MEASURED | RE-MEASURED AND FIXED r414. THE v1 INSTRUMENT WAS WRONG IN BOTH DIRECTIONS and its 33/20 numbers are withdrawn: it ignored each world's own `tune` (layered OVER the theme at track.js:1126), so BIRCHLAND CRESTS was counted inside the pine cluster while carrying a full dawn palette — OVERSTATING sameness; and it missed the twelve generated harbour worlds AND six themes assigned outside the literal (`THEMES.riviera = {...}`, `THEMES.savanna = {...}`, four more), which came back as dash-keys and read as clusters that do not exist. A FOURTH TRAP, recorded for the next session: `src/world/themes.js` and `src/world/levels.js` exist, are tracked and are full of plausible palettes, and NOTHING IMPORTS THEM — an abandoned split last touched at r334; `src/track.js` is the live table. v2 (tools-scratch/dbg-lookcensus2.mjs) stops pattern-matching the data and EVALUATES it in a sandbox, and reports the honest number: 78 worlds, 45 themes, 43 DISTINCT LOOKS, 56 worlds sharing a look. GRANITE NARROWS (the owner's own frame) was pixel-kin to TORRI CORSA. THE FIX: `LOOK_VARIANTS` in track.js gives each world in a cluster its own HOUR AND AIR, layered UNDER the world's own tune so a hand-authored palette always wins; one anchor per cluster keeps its look. Daylight and blue skies only (r406), no `dusk`, no foliage tint touched (7.14), no geometry moved. AFTER: 78 DISTINCT LOOKS of 78, zero shared, verified in the running game with a pageerror listener as well as in the parser. Closes the queued HIGHCROWN PEAK white-horizon report on the way past. | FIXED AND DEPLOYED r414 |
| E-12 | "Some places at remixed up. I'd like the field clean without trees. And add more field" (2026-09-11, frame r412 CIDER LANE) | THE FIELD IS A FIELD, NOT A CLEARING IN A WOOD. Three things in one sentence, and the frame shows all three: (a) MIXED UP — the scene reads as a jumble: a single large tan conifer stands alone in the middle of the open green field beside the road, with terraced gold rows and a dense multi-colour conifer wall immediately behind it. Nothing about that placement reads as designed; (b) CLEAN FIELD — the open grass beside the road MUST carry no trees. A tree standing in open field is the defect, not the density of the wood; (c) MORE FIELD — the open band between road and treeline is too narrow. The wood crowds the carriageway and the field is a strip; the owner wants the field to be the foreground and the forest pushed back to a treeline. THIS IS THE THIRD TIME THE OWNER HAS ASKED FOR OPEN GROUND (E-10 wide fields, the r412 open-field pilot, now this), so it is a standing preference about the game's look and not a one-world note. Related to E-11 (sameness): a conifer wall at the verge is exactly what makes every world read alike. MEASURE FIRST: per world, the distance from the road edge to the first tree (the treeline), and the count of trees standing INSIDE the open band. | OPEN | ROOT CAUSE MEASURED, FIRST FIX REVERTED AS TOO SLOW (2026-09-11). THE CAUSE IS CERTAIN: the theme already declares `treeBelt: [40, 140]` — a 31 u open field past the 9 u road edge — and the `trees:*` systems respect it (nearest birch 15.9 u, oak 16.0, larch 16.9, pine 21.9). THE VERGE CARPET DOES NOT: `_buildForestCarpet` places via `trackSpot(1, 38)`, a band hard-coded one metre off the road edge that never reads `treeBelt`, and it puts 5,448 instances within 60 u and 946 INSIDE 20 u, nearest at the very edge. That wall, the lone cone in the open grass, and the missing field are all that one call. Widening the belt would have changed nothing. THE FIX THAT FAILED, recorded so it is not retried blind: deriving the near pad from `treeBelt` is correct in RESULT and unaffordable in COST. Every sample in the ring, accepted or rejected, runs a `_nearestSample` lap search, a 4-point slope test and five ground seats; the ring rejects any slope past 50 deg. The road corridor is FLATTENED, so a band at the verge is accepted cheaply, while a band at 28-65 u sits in unflattened hill terrain where most draws are slope-rejected and the sampler burns its `count * 3` budget. MEASURED: base builds CIDER LANE in 28 s, the patched version had not finished at 170 s. Two wrong guesses were made first and are recorded as such — that `widthAt` was unavailable at that point in the build (false: the carpet runs at 13170, the width profile at 7208) and that the band had been compressed (true but not the cause; preserving its width did not help). WHAT A WORKING FIX NEEDS: to move the trees out WITHOUT moving the sampling into steep ground — e.g. sample in the cheap near band and transform the lateral, scale the instance count to the band actually used, or exempt the carpet from the slope test where the ground is field-flat. Not attempted blind. | OPEN, CAUSE PROVEN | CORRECTION, 2026-09-11 — THE DIAGNOSIS ABOVE WAS WRONG AND THE BUILD WAS NEVER SLOW. A `pageerror` listener returned `ReferenceError: T is not defined` in one attempt: the patch read `T.treeBelt` inside `_buildForestCarpet`, where the theme is `this.T`. THE WORLD NEVER BUILT — it threw on load. Every conclusion drawn from the timings is therefore void: there was no slope-rejection cost, no band-compression cost, and the "28 s base vs not-finishing-at-170 s" comparison was measuring a crash, not a slowdown. The claim written into the r413 HANDOVER that "the failure is the harness, not the game" was also wrong; it was this bug. THE LESSON, and it is the same one as the wrap-count and the drive-probe: ASK WHETHER THE THING RAN BEFORE EXPLAINING HOW IT RAN. Four hypotheses were spent on why a build was slow when twenty seconds of `page.on("pageerror")` said it never completed. MEASURED AFTER THE ONE-WORD FIX (`this.T`): CIDER LANE carpet trees near the road 5,448 -> 1,008, nearest 9.0 u -> 25.7 u, inside 20 u 946 -> ZERO. PINE VALLEY, the forest control, is unchanged at 5,129 / 9.2 u / 986 — a declared clear field loses its verge wall, a forest keeps it, and r368 stands. | FIXED AND DEPLOYED r413 |
| E-13 | "Create farmland themed tracks" (2026-09-11) | MORE FARM COUNTRY IN THE GAME. The fourth consecutive owner sentence asking for open field (E-10 wide fields, the r412 open-field pilot, E-12 "add more field", now this), so it is a settled direction and not a one-off. WHAT IS THERE NOW: three `farmland` worlds (HEDGEROW DASH 31, AERODROME CIRCUIT 35, ORCHARD PARK 43), two `harvestvale` (HARVEST RUN 70, CIDER LANE 71) and a `vineyard` pair — six or seven worlds of field country out of 78. THE CONFLICT, RAISED NOT TRADED (K-16): "create" reads as ADD, and CLAUDE.md §9 rule 5 says world count, chapters and star ratings are unchanged — adding worlds re-prices the career, because chapter gates are fractions of a chapter's stars and `starCost` prices from the career rung. The C-4 scope amendment permits REDESIGN of track geometry, length, curvature and scenery freely, so the reading that satisfies both is to BUILD THE FARMLAND TRACKS OUT OF THE FIELD-COUNTRY WORLDS THAT ALREADY EXIST — re-author them to the open shape E-10 measured (long sweeping bends in the R60-200 band, the 9 u width they already have, no walls), which is what "a farmland track" means as a thing to drive. Recorded as the assumption; if the owner wants the roster itself to grow, that is his call and a different build. | OPEN, ASSUMPTION STATED | BUILT r417, AND THERE WAS NOT EVEN ONE FARMLAND TRACK TO START FROM. The `farmland` route was a hedgerow POLYGON (14 control points at field boundaries, its own comment calling it 'a chain of short headland straights meeting at bends the hedge hides') and HARVEST RUN carried `route: 'farmland'` — so BOTH farmland worlds were the SAME LAP, the same corners in the same order. Re-authored with the SAFARI PLAINS grammar (r412): a closed loop whose radius varies as a few low harmonics, so curvature is continuous — no straight to end, no corner to arrive at. HARVEST RUN gets its own `harvestvale` route with a different harmonic mix. THE PARAMETERS WERE SEARCHED, NOT GUESSED: the first hand-picked attempt made it WORSE (69.6 pct dead) because I had confused the loop's radius with its radius of CURVATURE — a smooth 950 u circle IS dead ground and the metric was right; 150,000 candidates were then scored offline against SAFARI PLAINS' measured bands, seconds and no browser. MEASURED IN GAME: HEDGEROW DASH dead ground 45.2 -> 23.7 pct, open sweepers (R 200-800) 37.9 -> 72.1 pct, tight (R<60) 2.2 -> 0 pct, median radius 714 -> 544; HARVEST RUN 45.2 -> 22.0 / 37.9 -> 73.3 / 2.2 -> 0 / 714 -> 575, and now a DIFFERENT lap. Pilot for comparison: SAFARI PLAINS 18.0 / 70.4 / 0.4 / 539. The offline model predicted 24.6 and 24.2 against the game's 23.7 and 22.0, so the search can be trusted for the remaining field worlds — the `vineyard` pair (VINEYARD VELOCE, CIDER LANE) is still one lap between them and is the next one. ROSTER STAYED AT 78 (K-16). | BUILT AND DEPLOYED r417 | r418 FINISHED FIELD COUNTRY, AND FIXED THE INSTRUMENT THAT MEASURED IT. (a) THE METRIC WAS COUNTING CORNERS AS STRAIGHTS: `dead_over800` scores every station over an 800 u radius, and a road that CHANGES HAND passes through infinite radius at every inflection — measured on one candidate, 79 pct of its 'dead ground' sat within two stations of a curvature sign change. `_routecurve` now takes the curvature sign too and reports `straight` (dead that is NOT an inflection) apart from `inflection`. The old farmland lap was then re-measured IN GAME (checked out of git, not reasoned about) because a published number had to be right: before r417 45.2 pct dead = 40.0 straight + 5.2 inflection; after, 23.7 = 17.0 + 6.7. r417's claim SURVIVES and sharpens — genuine straight fell 40.0 -> 17.0 pct. (b) CIDER LANE carried `route: 'vineyard'`, which is THE OWNER'S OWN HAND-DRAWN LOOP, so two worlds drove one drawing; the drawing does not move and CIDER LANE gets `orchardrows`, aimed at the owner's FAMILY rather than its open-field siblings: 21.4 pct sweepers against their 4-5, median radius 415 against 544/575, zero tight. FIELD COUNTRY IN GAME NOW: VINEYARD VELOCE (the drawing) 6.7 straight / 33.6 sweep / p50 269; CIDER LANE 14.3 / 21.4 / 415; HARVEST RUN 14.2 / 4.7 / 575; SAFARI PLAINS 13.1 / 11.1 / 539; HEDGEROW DASH 17.0 / 4.2 / 544. TWO THINGS STATED, NOT BURIED: CIDER LANE was borrowing the best lap in the game and now has a roster-normal one (14.3 vs 6.7 straight), so de-duplicating cost it something and the owner may prefer the duplicate; and THE GENERATOR HAS A FLOOR THE HAND-DRAWING BEATS — recalibrating the offline search to the game (it sampled 300 stations where the game samples 900; predicted 7.0 straight, measured 14.3) put the best achievable at ~16 pct, while the owner's lobed loop reaches 6.7. More laps of that quality want drawing, not search. | r418 BUILT AND DEPLOYED |
| E-14 | "Remove the new tag and update the map s of each track. They don't match the current now" (2026-09-11) | TWO THINGS, ONE CARD. (a) THE NEW TAG: `main.js:4675` renders `<div class="wc-new">NEW</div>` whenever a level carries `fresh: true`, styled at index.html:796. Worlds still carry the flag long after they stopped being new. Delete means delete (working rule 2): the render call, the CSS rule, and the data flag if nothing else reads it. (b) THE MAPS DO NOT MATCH THE TRACKS, provably rather than impressionistically. The card draws `_drawCircuitMap(canvas, lv.route || lv.theme)` = `circuitPoints(key)`, and `circuitPoints` HAS EXACTLY ONE CALLER — the map. The TRACK builds its centreline at track.js:6872 from the same raw points and then applies three things the map does not: `routeFlipX` MIRRORS the plan (11 worlds — every left becomes a right, so the card shows the track's mirror image), `routeReverse` RUNS IT BACKWARDS (5 worlds), and the centreline is a closed `CatmullRomCurve3` in CENTRIPETAL parameterisation passing THROUGH the control points, while the map is a quadratic-midpoint path that only approaches them and rounds the corners off. So the card is a softened caricature of a shape that, for 16 of 78 worlds, is mirrored or reversed as well. ALSO FOUND: GLACIER COL declares route `glaciercol`, which is NOT in CIRCUITS, so it falls back to `CIRCUITS.forest` — but the TRACK takes the same fallback, so the map is honest and the WORLD is the bug: GLACIER COL drives PINE VALLEY's centreline. Logged separately, not fixed in the map build. THE FIX: draw the map from the same transformed points and the same curve the track is built from. | OPEN, CAUSE PROVEN | FIXED r415. `routePlanPoints(level)` now owns "what shape is this lap" (raw points + flipX + reverse) and BOTH the builder and the card ask it — the transform lines moved out of the builder rather than being copied, so the two cannot drift apart again, which is how this bug existed at all. The card then builds the same closed centripetal CatmullRom. MEASURED with a negative control that re-implements the old path (mean nearest-point distance to the built track's own `center`, both normalised into a unit box): RAZORBACK MOUNTAIN (flipX) 0.0919 -> 0.0008, CITADEL BAY (flipX) 0.0566 -> 0.0035, HARVEST RUN 0.0128 -> 0.0010, TORRI CORSA 0.0125 -> 0.0016, PINE VALLEY 0.0111 -> 0.0009. Eleven of twelve worlds measured improved, 2x to 115x. THE TWELFTH IS REPORTED, NOT BURIED: GRANITE NARROWS is a wash (0.0119 -> 0.0126) because three things can still move a centreline AFTER the curve — the editor warp, the opt-in T-01 weave (7 worlds), and a kink-relaxation pass that pulls any station turning over 20 deg toward its neighbours' midpoint. That pass fires only on worlds with authored cusps; for those the card draws the AUTHORED shape and the game drives a cusp-relaxed version, ~1.3% apart — one pixel on a 72 px canvas. Running 600 relaxation passes per card for 78 cards on menu open is not worth a pixel, so it is recorded rather than chased. THE NEW TAG is deleted properly (render call, CSS rule, grep clean); the `fresh` FLAG stays because a second reader, `freshRegions`, sorts regions with new worlds to the front of the board — the owner asked for the tag, not the ordering. | FIXED AND DEPLOYED r415 |
| E-15 | "Savage mode needs to be impossible to beat. Now it is easy" (2026-09-11) | THE TOP TIER HAS NO PACE OF ITS OWN, AND IT WAS ALREADY WRITTEN DOWN. `DIFFS` (main.js:196) sets `aiSpeed` to 1.06 on NORMAL, HARD **and** SAVAGE — the entire tier difference lives in `aiCorner` (0.65 / 0.72 / 0.80) and in aggression. The width-pinch cap at vehicles.js:5739 then `Math.min`s corner speed with a pace-only term that DISCARDS aiCorner, so at every narrow or pinched station the three tiers converge. Measured at r409 by the four-tier suite: median rival distance 402 / 402 / 401 m across NORMAL, HARD and SAVAGE — three rungs, one pace. SAVAGE is NORMAL's speed wearing more aggression, which is exactly why it is easy. The r409 note calls the fix (carry `sqrt(aiSpeed*aiCorner)` through the cap) correct but deferred, because it lifts HARD ~5% and pushes HARD past the clean-winnable bound on FURKA RIDGE, so the tier table must be retuned in the same change — "an AI-pace build of its own", H-8. That build is what the owner is now asking for. ONE THING PUT TO HIM RATHER THAN ASSUMED: LITERALLY unwinnable makes SAVAGE's stars unobtainable and is a design trap, so the target taken is BRUTAL — winnable only in a fully-upgraded top-tier machine, driven cleanly, and not reliably even then. If he means literally impossible, that is his call. | OPEN, CAUSE PROVEN, ASSUMPTION STATED | FIXED r416, AND THE FIRST TWO LEVERS MISSED THE POINT — recorded because the third is the lesson. (1) The pinch cap now carries the tier: `cornerCap` = sqrt(aiSpeed*aiCorner) NORMALISED TO NORMAL, clamped [1.00, 1.12]; the floor keeps EASY untouched and the ceiling exists because unclamped SAVAGE reached 1.253 and WENT BACKWARDS on the narrow world (FURKA median 275 vs HARD 282 — a pinch is a physical width). (2) aiCorner 0.80 -> 0.92, not 1.02: 1.02 measured worse for the same reason, and the r284 ceiling (player tyres obey a_lat <= 4*grip) still forbids a field planning far past 1.0. Plus `mistakeMul` 0.10 — SAVAGE rivals err at a tenth the roster rate. (3) THEN THE MEASUREMENT SAID BOTH HAD MISSED: with them landed, PINE's MEDIAN rival went 381 -> 405 while THE BEST RIVAL SAT AT 415 BOTH TIMES. The player races the leader. The cause was one condition — rival nitro is gated on `gap > 0.004`, and gap is player-progress-minus-mine, so a rival only boosts when BEHIND the player and THE LEADER NEVER BOOSTED AT ALL. Nitro was a pure catch-up mechanic. `freeBoost: true` on SAVAGE lets the front of the field use the same nitro the player has. MEASURED against one baseline run: PINE median savage 381 -> 446; PINE best rival 415 -> 475 (clean stand-in 372, now losing by 22%); PINE ladder 401/397/381 INVERTED -> 395/401/446 monotone; FURKA median 277 -> 290; PINE 75%-throttle gap -29.2% -> -46.5%. New law 4c in test-difficulty asserts SAVAGE BEATS a clean drive — the old SAVAGE law was only a floor ('brutal, not a wall'), which the easy tier satisfied happily. TWO FAILURES REMAIN AND ARE NOT THIS BUILD'S: EASY casual-winnable is red on both worlds in the BASELINE run too (PINE -5.3% then / -5.0% now, FURKA -7.4% / -6.9%) — baseline 3 failed, now 2 failed, both pre-existing and both slightly improved. NOT CLAIMED: literally impossible, which would make SAVAGE's stars unobtainable; and the stand-in is a drift-less robot, so a drifting owner will be closer than 22%. | FIXED AND DEPLOYED r416 |
| E-16 | "Combine the worlds and just draw" (2026-09-12) | ANSWERS THE TWO THINGS PUT TO HIM IN THE SAME BREATH, and is read as: (a) COMBINE — do not grow the roster to make room for new tracks (the K-16 conflict: "create farmland tracks" vs CLAUDE.md §9 rule 5). Where worlds duplicate each other, CONSOLIDATE rather than add. Measured state after r418: 28 worlds still share 12 route shapes, the largest cluster being four worlds on `caps`. (b) JUST DRAW — stop reaching for the parameter search. r418 measured why he is right: the offline generator has a floor around 16 pct genuine straight, while his own hand-drawn `vineyard` loop reaches 6.7 pct with 33.6 pct sweepers, the best lap in the game. Hand-authored routes, not searched ones. RECORDED AS A READING, not a certainty: "combine the worlds" could also mean fusing two worlds into one larger one, which would change world count and career pricing and is therefore the interpretation NOT taken without him saying so. | OPEN, READING STATED |
| E-17 | Standing report: "the owner is on r405, r406 is live" (service worker serves a stale build) | ROOT-CAUSED AND FIXED r419. NOT the update machinery — `src/offline.js` has done `updateViaCache: 'none'` + an explicit `update()` + a re-check on visibilitychange + auto-reload on controllerchange since r289, and a first harness that registered a BARE worker reproduced a straw man. THE REAL FAULT IS THE PRECACHE: `cache.add(u)` in install is an ordinary fetch, so the browser's HTTP CACHE satisfies it, and GitHub Pages serves with a max-age — so a player who opened the game shortly before a deploy precaches THE PREVIOUS BUILD'S BYTES UNDER THE NEW CACHE NAME, and the cache-first fetch handler then serves them forever. A cache called ignite-rally-r406 full of r405, which is the report verbatim. REPRODUCED (tools-scratch/_swupgrade.mjs: install build A, swap the server to build B, reload while reporting DOM / module / cache): plain `cache.add` left the cache key on rBBB with the DOM and module still rAAA through all six reloads; `cache.add(new Request(u, { cache: 'reload' }))` reaches build B ON RELOAD 1. CORE only — EXTRA is 58 previews, 1.15 MB, that never change. A harness bug was found and fixed on the way (its success check tested a field the diagnostics rewrite had removed, so it printed STILL STALE over data showing the upgrade working) — a tool that lies is worse than no tool. | FIXED AND DEPLOYED r419 |
| E-18 | "You can also twist the tracks make them drive the other world and change the scenery. Woala" (2026-09-12) | THE CHEAP ANSWER IS THE RIGHT ONE, and it was already built. A shared shape becomes a different place to drive by (a) TWISTING it — `routeFlipX` mirrors the plan, `routeReverse` runs it the other way, both together are the r327 "fourth distinct lap"; (b) running it under ANOTHER WORLD'S theme; (c) its own palette (r414). MEASURED BEFORE BUILDING (tools-scratch/dbg-twist.mjs): of the 28 worlds sharing 12 route shapes, 27 ALREADY carry a distinct twist or a different theme. EXACTLY ONE drove a lap another world drives the same way — LARCH GOLD, untwisted on `deepwood`, identical to DEEPWOOD TRAIL and confirmed independently by curvature (both 8453 u, median radius 547, 23.4 pct straight, 8.9 pct sweepers). FIXED r420 with the owner's own trick: `routeFlipX + routeReverse`. VERIFIED THE WAY A TWIST HAS TO BE — a mirror-and-reverse preserves every radius, so the curvature histogram is identical BY CONSTRUCTION and cannot answer the question; what changes is sequence, handedness and outline, so those were measured (tools-scratch/_twistcheck.mjs): corner handedness now agrees station-for-station only 41.8 pct of the time (50 pct = unrelated, so it is anti-correlated, which is what a mirror does) and the plan outline distance is 0.0346 against the ~0.001 that "same shape" scores. A FIRST PASS OF THIS ROW WAS WRONG AND IS CORRECTED HERE: it also named GLACIER COL as an untwisted duplicate of PINE VALLEY. It is not — it measures lap 5239 / p50 185 / 43.6 pct sweepers against PINE's 6800 / 421 / 17.7. The census had missed `CIRCUITS.glaciercol = composeRoute(...)`, assigned AFTER the literal, which is EXACTLY the trap the look census hit with `THEMES.savanna = {...}` in r414 — second table, same mistake. Both censuses now fold in post-hoc assignments. THE STANDING LESSON: in this file a table is not finished at its closing brace; grep for `X.key =` before trusting any literal-only parse. Two clusters still share a theme (caps: TOUR DES CAPS + TERRAZZA ALTA; autodromo: AUTODROMO VELOCE + SALINE SPRINT) and are LEFT ALONE deliberately: both pairs already differ by twist and by r414 palette. | FIXED AND DEPLOYED r420 |
| E-19 | "Remove or rework the maps" (2026-09-13) | THE CIRCUIT-OUTLINE BADGE ON EACH WORLD CARD, a 72x52 canvas sitting on top of the preview photo. REMOVED r421, and the choice is not laziness — the thing had been WRONG TWICE. Until r415 it drew raw control points with no `routeFlipX` or `routeReverse`, so eleven worlds showed the mirror image of their own lap. And r415 fixed only ONE of its two call sites: `_markCurrentCard`, which runs every time the highlight moves, kept passing `lv.route || lv.theme` — a STRING — into the new level-taking signature, so `routePlanPoints` read `undefined` and every redraw fell back to `CIRCUITS.forest`. Since r415, moving the selection has been repainting cards as PINE VALLEY's loop. That is a regression I shipped and the gate could not see it, because no suite renders the card highlight. A 72 px outline has to be RIGHT to be worth anything; twice wrong makes it decoration with a defect rate, and the owner offered the exit. Deleted properly: both call sites, the `_drawCircuitMap` method, the now-unused `routePlanPoints` import in main.js, two CSS rules, and the stale comments that named it. `routePlanPoints` itself STAYS — the track builder is its real caller and it is the single source of truth that killed the flip/reverse duplication. The preview photo is untouched. | REMOVED AND DEPLOYED r421 |
| E-20 | "Rename the tracks" (2026-09-13) | ALL 78 WORLD NAMES, and it lands on an existing rule rather than taste: CLAUDE.md §7.10 forbids real circuit, city, mountain, brand and driver names in stage data, and acceptance query R9 is a string scan for exactly that. AUDIT FIRST, then rename — the criterion is the rule, not novelty, because several names are the owner's own (SERPENT PASS came off his hand-drawn sheet at r327) and those are not mine to throw away. Recorded before any code; the audit and the rename ship as r422, separately from E-19, because a card-rendering change and a stage-data change are two systems (working rule 1). | AUDITED AND RENAMED r422. Census (`tools-scratch/dbg-namecheck.mjs`, every world name checked against a list of real circuits, cities, regions, mountains, marques and drivers): 78 worlds, 7 flagged. Renamed: PRINCIPALITY STREETS->CROWN HARBOUR, ARDENNES SWEEP->BEECHWOOD SWEEP, CINQUE BORGHI->HILLTOWN STACK, AEGEAN BLUE->WHITEWASH BAY, DALMATIA DRIVE->LIMESTONE COAST, COTE D AZUR->SAPPHIRE SHORE, LIGURIA STAGE->OLEANDER STAGE. SECOND PASS over every display string (182 fields, not just the 78 names) found five more, which the hand-written match list had missed: AMAZON RAPIDS->GREENWATER RAPIDS, COSTA BRAVA->WINDWARD COVES, region AMAZON->GREENWATER, region NEO-KYOTO->NEON DISTRICT, region+chapter MEDITERRANEAN->INLAND SEA, region RIVIERA->ALBAROSA and chapter THE ITALIAN RIVIERA->THE ALBAROSA COAST. test-naming.mjs claimed in its header to scan chapter titles and never did (it walked LEVELS only); it now scans CHAPTERS, which is how THE ITALIAN RIVIERA survived since r334. Twelve strings changed; R9 passes on the roster for the first time, over the fields it always claimed. The other 71 names are legal and several are the owner's own, so they stand — a full-roster rename is available on his word. NOT TOUCHED: 15 route/theme KEYS are real-world names (turini, pikes, estonia, principality, ardennes, tremola, furka, dolomiti, liguria, aegean, dalmatia, azur, riviera, genova, sanremo), but `seedForLevel` hashes `epoch:id:route||theme`, so renaming a key re-rolls that world's entire scatter — a stage-data rewrite masquerading as a rename. Keys are not stage data on screen; recorded as K-17 rather than silently changed. |

## F. Plants, scenery and structures — the HRD hard road rules

HRD-1..6 are recorded in CLAUDE.md §7A and enforced in the blocking gate.
Summarised here for citation; that table remains the operational text.

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| HRD-1 | "RULE: nothing stands at the middle of the road" | No body of any class intersects the drivable width at any station, on any leg. Gate: test-nothing-on-road. | LIVE |
| HRD-1a | AMENDMENT to HRD-1, on the E-9 hazard frame (2026-09-10) and the owner's "Yes" to literal blockers | MEASURED FIRST, AND IT CHANGED THE AMENDMENT. HRD-1 was never enforced against everything: `_buildProps` already stands CRATES, CONES AND BARRELS on the drivable surface across the whole roster, on purpose — "NOT blockers, a car drives straight through one and accelerates" — and `test-nothing-on-road` COUNTS them and prints the total rather than asserting against them. So the operative law, already in force and only now written down, is: HRD-1 forbids a body that STOPS THE CAR. A pass-through smashable is not one, and the frame's crates and drums are exactly that class. THE AMENDMENT IS THEREFORE A BOUND, NOT A HOLE: any body inside the drivable width MUST (a) be destructible and yield on contact — no velocity stop, no hard collision response, the car leaves faster than a blocker would let it; (b) stand <= 0.6 m for a smashable, <= 0.25 m for a CHARGE PLATE, so nothing lifts a wheel or hides behind a crest; (c) be telegraphed at least 60 m ahead on the racing line, readable at the world's design speed on every palette (§7.9); (d) never sit in a gate clear zone, an apex band, a kicker landing zone or the finish run-out; (e) charge plates at most one per 150 m of road. Everything else HRD-1 forbade in the carriageway still fails hard — a house, a rock, a tree, a fence, a sign, a barrier, anything that stops a car. `test-nothing-on-road` gains the charge plate in the SAME shape as the existing prop class: counted, with (a)-(e) asserted as bounds. No by-name pin and no waiver. | SPEC'D, NOT BUILT |
| HRD-2 | "This road needs widening" | Curvature widening, bounded by W-CURVE-01.2 (±30% of approach width). | LIVE |
| HRD-3 | — | Drivable half-width never below 3.0 u. | LIVE |
| HRD-4 | — | `|widthAt(i+1) − widthAt(i)| ≤ 0.6 u` — the road tapers, never steps. | LIVE |
| HRD-5 | "there is always a mountain range from one of the sides of the road", scoped to "steep mountains… there can be field roads. That is fine." | On steep-mountain terrain every station carries rising ground on at least one side. | LIVE |
| HRD-6 | "No road floats or is on a ridge" | No station has falling ground on both sides; no road edge stands unsupported. | LIVE |
| HRD-7 | "Fix all trees that are in the house — this is A HARD RULE — swipe across whole game" (2026-09-09) | No tree, bush or tuft may stand inside a structure footprint, on any of the 78 worlds. The plant loses, not the building. MEASURED: 41,195 offenders across 67 of 78 worlds — 36,329 forest carpet, 4,619 ground cover, 247 solid stand. Worst SEA CLIFF RUN 16,322 and CLIFF KNOT 14,622. Needs a blocking-gate suite, as a hard rule. | OPEN |
| F-1 | "Pine trees are never not green… Apply this across the game." (§7.14) | Every conifer renders green foliage on every palette. LARCH IS EXEMPT by the owner's own word. | LIVE |
| F-2 | "Trees in the ice?? Fix" (2026-09-09, GLACIAL PASS) | Nothing grows inside a cliff ribbon on any cliffWalls world, and on the ice-walled worlds nothing grows in the canyon or up its ice flanks. Snowy forests (FROST PEAK) are untouched. | LIVE (r407) |
| F-3 | Structures (§7.13) | No structure base more than 0.1 m above terrain at any corner. NOTE: a race log showed `structure-hover` at 1.8 m on 2 structures while `test-nothing-floats` passed green — the gate has a blind spot for this class. | OPEN |

## G. Camera

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| G-1 | §6.4 | One camera per race; mode changes only in the pause menu. No automatic cuts. Probe raycasts terrain, canopy AND building colliders every tick including airborne. | LIVE |
| G-2 | §3.9 | Clamped closer than 6 m to the car, the camera RISES toward top-down instead of pulling in. | LIVE |
| G-3 | "There is camera shaking after the tunel" (2026-09-09) | The exit-side hand-off must be as steady as open road. r398 fixed the entry pair; its own note records the exit reaching +33 u over the crown, capped to ~+7 — a cap, not a cure.  RE-REPORTED "Still shaking under a tunel" (2026-09-11, r412) — and UNDER, not after, which narrows it. CAUSE FOUND IN THE CLAMP ITSELF: the bore branch set `fy = tk.center[ci].y`, the centre height at an INTEGER station index. That is a staircase — as the camera runs the bore, `ci` steps to the next sample and the floor jumps a whole station's rise in one frame, taking the ceiling with it since both are derived from `fy`. The view twitches wherever the bore is not dead level. THE SAME TRAP IS ALREADY DOCUMENTED ON THE PHYSICS SIDE ("groundHeightAt is a staircase between samples; at racing speed the car crosses a step every three frames") and was fixed there by moving to the fractional read — the camera was never given that treatment. That is why r398's entry-pair fix and its +7 u exit cap did not cure it: neither touched the staircase, and the note already said a cap is not a cure. FIXED r413: `fracIndexAt` + `groundHeightAtFrac` at lateral 0 (no bank term at zero, no ramp in a bore) gives the same height the old line wanted, sampled continuously. | FIXED AND DEPLOYED r413 |
| G-4 | C-CAM-01 (Race Integrity) | No frame majority-occluded by vegetation between camera and car; fade the intersecting instances. | OPEN |
| G-5 | C-CAM-02 | The camera collides with world geometry; backfaces never render full-screen. | OPEN |
| G-6 | C-CAM-03 | Airborne: car inside the central 60% of the screen; speedo shows ground speed, never 0; gear holds, never N. | OPEN |
| G-7 | FALKEN RIDGE frame (2026-09-09) | Tree crowns must not fill the frame at the camera. The canopy guard reads crown radius and top from `camTrees`, which the VERGE ring alone populates — the same registry gap as B-7(a). | OPEN |

## H. AI and difficulty

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| H-1 | §5 standing decision | DELETE THE RUBBER BAND. Removed in r313; `rubberBand`/`bandUp` are gone from EnemyCar. Convergence exists ONLY as the pressure rival's ±3% lease. Any future request to "bound rubber-banding" has nothing to bound — find the real cause. | LIVE |
| H-2 | "Opponents should match my car's strength. Needs to be a constant battle… Not me going away from them always" (2026-09-09) | `machineParity` matches grid pace to the player's machine class. | LIVE (r404) |
| H-3 | "Current hard level should be normal. Make 2 more harder levels where cars are more aggressive." (2026-09-09) | Ladder: EASY / NORMAL (= old HARD) / HARD / SAVAGE, ordered by `tier`. aiSpeed held flat at 1.06 across the top three because raising it INVERTS tier order via the `v > maxSpeed*0.55` nitro gate. Edge comes from aiCorner and aiAggression. | BUILT (r409) |
| H-4 | "Make it adaptive. If I'm driving good make them more angry" | Adaptation rides AGGRESSION ONLY, never pace — a live speed band is H-1's rubber band. Form is the player's progress lead over the best rival, smoothed 4 s, scaled by tier. EASY never gets angry. | BUILT (r409) |
| H-5 | §5.3 | FOLLOW → SETUP (≤1.5 s) → COMMIT (2.0 s) → CLEAR/YIELD. DEFECT FOUND in the owner's race log: SETUP steers to `aheadCar.lateral + side*3.5` while `aheadCar` requires `|across| ≤ 3.4` — 0.1 m outside its own detection window — so the rival loses sight of the car it is passing, drops to FOLLOW, and `_ovSetupHeld` resets, making COMMIT unreachable. Measured 18 flaps in 1.3 s; a pass never completes. | OPEN |
| H-6 | §5.4 | Token arbiter: ≤1 rival before GO+20 s, ≤2 after, rotate 6 s. OBSERVED: the same two cars re-acquire on every lapse — the cap holds but rotation does not. Top tiers raise the late cap to 3 (H-3). | LIVE / partly OPEN |
| H-7 | G-AI-01 (Race Integrity) | Bound rubber-banding both directions. NO MECHANISM EXISTS (H-1). The observed bunching and the 2nd→8th respawn swing need root-causing; H-5 is the prime suspect. | HELD — see L-5 |
| H-8 | measured r409 | THE PINCH CAP IS TIER-BLIND. `EnemyCar` caps corner speed through a narrow station at `(16 + 3.6*w) * pace * progRamp` — it carries the persona's pace and the roster ramp but not the difficulty. That was harmless while tiers were separated by aiSpeed, which the straights expressed; it is not now, because aiSpeed is flat at 1.06 across the top three (H-3) and the whole tier difference lives in aiCorner, which this cap discards. MEASURED: median rival 402 / 402 / 401 m on NORMAL, HARD and SAVAGE — three rungs, one pace. On FURKA RIDGE the three read 285 / 275 / 281 against a ±6 tolerance, so the suite's law 1 ("rival pace rises with difficulty") is now a coin flip there and goes red when the noise lands the wrong way. That red is r409's own, and it is left red rather than loosened: it names this defect exactly. The fix (carry sqrt(aiSpeed·aiCorner) normalised to NORMAL) was written and measured; it lifts HARD's field ~5% and takes HARD past the clean-winnable bound on FURKA RIDGE, so the tier table needs retuning in the same change. Held out of r409 rather than shipped half-done: the ladder's rungs differ in AGGRESSION, which is what the owner asked for, and in pace only on worlds without narrows. | OPEN |
| H-9 | measured r409 | EASY IS NOT CASUAL-WINNABLE BY THE TEST STAND-IN, AND HAS NOT BEEN FOR SOME TIME. The suite's law 5 (a 75% throttle drive within 2% of EASY's best rival) reads -5.3% on PINE VALLEY and -7.4% on FURKA RIDGE. PRE-EXISTING, not from this build: EASY's row (aiSpeed 0.74 / aiCorner 0.26 / aiAggression 0.65) is untouched by r409, and an A/B with the old 77 m grid restored reads -10.7% and -8.4% — worse, i.e. the r409 grid fix improved it. Prime suspect is r404 machine parity (H-2), which lifts the grid to the player's machine and only halves that on EASY (`parityCloseEasyMul` 0.5); a casual tier arguably should not chase at all. Not a deploy blocker — test-difficulty is outside the blocking gate's deploy set. | OPEN |

## I. Rendering, sky and readability

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| I-1 | "Shades needs to be consistent and constantly there" (2026-09-09) | One shadow law applied after every builder; wider, view-biased shadow box. | LIVE (r405) |
| I-2 | "Make blues skies and sun no fog" (2026-09-09) | Sky hem 0.14 (0.5 on dusk worlds), a real sun disc on the dome, ambient fog past everything reachable, haze-band curtains deleted, dome and stars follow the camera. | LIVE (r406) |
| I-3 | "Horizont should mit be as white" (2026-09-09) | The horizon band must not read white. Re-verify per theme on HEAD; where it still does, tint `skyHorizon` off pure white and/or narrow the hem — a per-palette decision, not a global. | OPEN |
| I-4 | "Also check for any unusually fog curtains" | Audit for any remaining curtain-class body (large, `fog:false`, inward-facing). | OPEN |
| I-5 | "There some wierd white horizontal lines" + "Remove the white triangle" + "Still see the lines" (2026-09-09) | ROOT-CAUSED r409: not scene geometry at all — the `#speed-lines` DOM overlay in index.html. Two `repeating-linear-gradient` families at 175° and 185° over `inset:0`, so each is a near-horizontal white stripe set and their 10° crossing reads as a long pointed sliver. A radial mask leaves the corners lit, which is the "white triangle". It is drawn OVER the HUD, which is why the sliver crossed translucent panels and no scene-child bisect ever found it. Fires from 95 km/h (`speedLinesFromKmh`), i.e. most of every race, while its CSS comment still said "past 150 km/h" — that stale comment is why five reports were chased into the world. Disabled by raising the threshold above reachable speed (see K-10). Earlier negatives stand and were not wasted: world-skirt 0 px, shadow acne, chairlift cables 0 px, rain, cloud-bank tails, haze bands — all ruled out, correctly. | FIXED (r409) |
| I-6 | §7.9 | Drop edges readable on every palette: the last 2 m before a drop uses the template's rock material. | LIVE |
| I-7 | P-PERF-01 (Race Integrity) | 60 fps sustained, no hitch above 33 ms. Measured 30-33 fps with 50-67 ms hitches. | OPEN |

## M. Economy, upgrades and career

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| M-1 | "Work on the game economy, upgrades, career building etc." (2026-09-10) | An open brief, not a defect report. Ground it in measurement before changing a price: `tests/test-economy.mjs` reads the constants where they are written and is GREEN on this base (upgrade steps 600/1100/2600/5100/8600, one line 18,000 CR, a strong race ~1,800, 110 races to max a car, 68 to own the roster). ECONOMY-PLAN.md's own standing advice is "not recommended: re-pricing anything" — the curve was measured into place at r148 and is defended by that test. So the work is where the plan says the gap is: REASONS, not money. | OPEN |
| M-7 | measured r410 | INCOME IS PAID FOR PLACING, NOT DRIVING. A staged 78-round career tallies race score at 84,916 CR of 253,318 — 33.5% — and the rest is flat: first clear 18.5%, podium 13.0%, championship points 11.9%, season prize 10.3%, finale double 7.4%, sponsor 2.8%, quest 2.5%. That 33.5% is FLATTERING, because the sim feeds a fixed race score of 4000; the live per-race census reads driving at 21-30% on most worlds and 2% on a finale (FALKEN RIDGE pays 2800 CR for a race that was 2% driving). M-2 lifts the driving share on 40 worlds; whether the flat side should also come down is a separate decision and is NOT taken here. | OPEN |
| M-8 | measured r410 | EARNING RATE IS A PROPERTY OF THE WORLD, NOT THE DRIVE. Same bot, shipped config, credits per minute: LANTERN QUARTER 67, EMBER PASS 102, RAZORBACK 110, CIDER LANE 119, REDWOOD 130, PINE VALLEY 152, COSTA BRAVA 200 — a 3x spread before finales, which pay 426. Score rate spans 0.76 to 4.96 points per second across worlds. So WHICH world you race moves income more than how well you drive it. Not yet root-caused: score comes from props, kills and style, and prop density is a world-build property nobody has levelled. | OPEN |
| M-9 | measured r410 | TERRAZZA ALTA scored 0 and paid 0 CR in 171 s of driving — the only world in the census that paid nothing at all. Either the centreline bot fails there or the world has no scorable content; unresolved, and quoted nowhere until it is. | OPEN |
| M-10 | measured r410, AFTER the threshold change | THE LAP FIX WORKED ON THE MIX AND BROKE THE RATE. Driving's share on worlds that flipped to 3 laps: PINE VALLEY 27% -> 86%, EMBER PASS 47% -> 79% — past even the 57% the A/B predicted, because a longer race also loses the CLEAN RUN bonus more often, which shrinks the flat half further. But credits per MINUTE fell with it: PINE 152 -> 50, EMBER 102 -> 91, against 128 on 1-lap REDWOOD and 180 on 1-lap FALKEN. The flat bonuses are paid once per RACE and a race is now 3x longer, so the 40 three-lap worlds are the WORST worlds to farm — a player optimising income would avoid exactly the worlds where driving matters most, which inverts the intent. PINE's per-race total even fell slightly (587 -> 552). CANDIDATE FIX, not taken without the owner: scale the per-race flat bonuses with lap count so a 3-lap race pays ~3x flat, making credits-per-minute roughly world-independent. That IS a re-pricing, which ECONOMY-PLAN.md advises against, so it is a decision and not a tidy-up. | OPEN |
| M-2 | §6.1c (r381), retuned r410 on the owner's pick | THE RULE HAD NEVER FIRED. `_lapsForTrack` races 3 laps under `lapsShortTrackU` and 1 over it, and the constant shipped at 3000 u — while a census of all 78 worlds reads min 4532 / p25 5783 / median 6935 / p75 8529 / max 12038. The shortest track in the game is 1.5x the threshold, so every world took the 1-lap branch and the 3-lap half of the r381 directive was dead code. MEASURED CONSEQUENCE, same world and bot with lap count the only variable (PINE VALLEY): 1 lap scores 883 in 227 s and pays 177 race CR against 430 flat — driving is 29% of the payout; 3 laps scores 2310 in 631 s and pays 462 against 350 — driving is 57%. `raceCr = raceScore × CREDIT_RATE × diffMult` scales with race length; PODIUM_CR, FIRST_CLEAR_CR and SWEEP_CR do not, so shortening the race silently halved what driving well is worth. Threshold now 7000 (owner's choice from the distribution, 40 of 78 worlds on 3 laps). No price changed. | BUILT (r410) |
| M-3 | ECONOMY-PLAN.md §2.1, shipped r161 | Contract rungs escalate per contract, persisted as `career.rungs = {contractId: rungIndex}`; absent means rung I. Bound: a full sweep of three rung-III contracts pays about one strong race, not three (asserted in tests/test-rungs.mjs). | LIVE |
| M-4 | ECONOMY-PLAN.md §2.2 | Rival signatures (blocker / late braker / opportunist) — behaviour, not stats, keyed to the driver. Bar: a blind test must identify which rival is which from the last third of a race, or the signatures are too subtle to have been worth the code. | OPEN |
| M-5 | ECONOMY-PLAN.md §2.3 | The daily line: one world per day, seeded from the date, fixed car and weather, one target time, NO credits — unpaid on purpose, so it never needs balancing against the campaign and never needs anti-farming rules. | OPEN |
| M-6 | ECONOMY-PLAN.md §3 | Acknowledgement, not content: name the moment (style events already detected), let one system react, and end the results card on the best thing that happened rather than a table. | OPEN |

## N. Track variants on existing worlds

| ID | Owner sentence | Rule | Status |
|---|---|---|---|
| N-1 | "Re use the tracks and redraw new racing scenes on the existing worlds. Make more jumps steep curves etc. Idea is to reuse the worlds for new tracks" (2026-09-10) | MOSTLY ALREADY BUILT — investigated before designing anything. A world is `{id, name, theme, region}` and the racing line is a SEPARATE named point list: `rawPts = CIRCUITS[level.route || level.theme]`, with `routeFlipX` and `routeReverse` modifiers already in place. 60 shapes serve 78 worlds. So a variant track is a new LEVELS entry carrying the SAME theme and region — all the terrain character, scenery and palette investment — with a different `route` naming a new hand-authored shape. No engine work is needed to make it possible; the work is authoring shapes and clearing §7. CAUTION 1: `seedForLevel` keys on `id` AND `route||theme`, so a variant gets its own seed and therefore its own terrain detail — same LOOK, not the same GROUND. Literal shared topography needs the seed keyed off the shared world instead (open question to the owner). CAUTION 2: the road conforms to terrain (§3.8) and HRD-5 builds flanks along the corridor, so a new line carves its own valley. CAUTION 3: "more jumps, steeper curves" is exactly where §7.5 min corner radius per template and §7.3 kicker landing zones bite — the validator is the constraint, not the authoring. | OPEN |

## J. Delivery

| ID | Rule | Status |
|---|---|---|
| J-1 | Every deploy bumps `build-tag` in index.html, the three `?v=rNNN` module URLs, and `CACHE` in sw.js. | LIVE |
| J-2 | `node tests/validation.mjs` MUST exit 0 before any deploy. A red blocks unless it is a recorded waiver with a ledger reference. | LIVE |
| J-3 | Every deployed build gets a narrative HANDOVER.md section above the previous one. | LIVE |
| J-4 | THE OWNER MAY NOT BE SEEING THE BUILD YOU SHIPPED. sw.js is cache-first for the navigation document, so a load renders from the old cache while the new worker precaches. Confirmed: the server served r406 while every phone frame read r405 across four loads. Fix: on activate + claim, tell the page and reload once at the menu. Do NOT make navigation network-first — with `ignoreSearch: true` that yields a fresh shell against stale modules. | OPEN |
| J-6 | Build practice, corrected 2026-09-10 | THE WORLD RNG IS PER-WORLD, NOT ONE GLOBAL STREAM. `worldSeed = seedForLevel(level)` (an FNV hash of epoch:id:route|theme) and `withSeed` swaps `Math.random` for the duration of that ONE world's Track construction, restoring it after. I had it recorded the other way — as a single stream where a changed draw count re-rolls every LATER WORLD — and acted on that reading more than once. What is actually true, and still binding: a draw added partway through a build re-rolls everything constructed AFTER it IN THAT WORLD, and since every world runs the same builder, that is a scatter reshuffle on all 78 at once. So new scatter is still written zero-draw (derive from an index already in hand, or place deterministically from the route) — but a NEW WORLD can be added without disturbing any existing one, which the old reading wrongly forbade. | LIVE |
| J-7 | Delivery practice, learned the hard way twice | A BLOCKING QUESTION KILLS A RUNNING GATE. The container suspends when the session goes idle, and an AskUserQuestion idles it exactly like an ended turn: the r411 gate died at 3 of 10 suites with no verdict while I waited for an answer. The earlier five deaths were the same mechanism from ending turns. RULE: never leave the blocking gate running across a question to the owner. Either ask BEFORE starting the gate, or let the gate finish first and ask after. The chained short background polls keep a gate alive only while the session keeps taking turns. | LIVE |
| J-8 | THE BLOCKING GATE REPORTED GREEN ON A RUN WHERE NOTHING WAS TESTED (found and fixed 2026-09-11) | The r411 gate printed "GATE: green — clear to deploy" across all ten suites at ONE SECOND each, against real timings of 24s / 342s / 151s / 2567s / 2025s / 209s / 40s / 12s / 211s / 17s. Cause: the suites fetch http://localhost:8901, nothing started it, every suite died on ERR_CONNECTION_REFUSED, and a stack trace carries no FAIL line and no "N passed, M failed" line — so the parser found no failures and scored each crash green. TWO signals existed and both were discarded: `runSuite` captured the child exit `code` and the caller destructured only `{out, secs}`; and the run produced no assertion output at all. The header's reasoning ("suites exit 0 even on FAILED, so parse verdicts, never exit codes") is true of a FAILING suite and does not carry to a CRASHING one. FIXED, three parts: (1) a non-zero exit is a hard failure — a crash is not a verdict; (2) green now requires positive evidence that the suite asserted something, the whole-suite form of tests/README rule 4 ("a check that matches nothing passes forever"); (3) the gate STARTS ITS OWN page server on 8901, reusing one already listening so a developer's server is not killed under them, and BLOCKS if it cannot. Verified by negative control: a deliberately crashing suite now prints BLOCK with both reasons and the run reports BLOCKED. CONSEQUENCE FOR THE LEDGER: any past green cannot be assumed to mean the suites ran — the timings in the log are the evidence, and a suite whose time is implausibly short was not a pass. | LIVE |
| E-21 | "the resets after a crash need fixing" (owner, r411) | A CRASH RESET SEATS THE CAR ON THE ROAD IT IS ACTUALLY ON. respawn()'s fallback clamped lateral to a fixed +-6, a figure predating per-station road widths, so on any road narrower than 6 u the car came back OUTSIDE the carriageway. MEASURED: IL VICOLO (3.04 u half-width, all 900 stations under the clamp) put a car that died at lateral 6/12/20 back 2.91/3.77/2.91 u off the road every time; PINE VALLEY at half-width 9 showed six clean rows and would have closed this as not reproducible. The clamp now follows widthAt(station) less 1.5 u, floored at 0. tests/test-reset.mjs races a wide AND a narrow world, crashing at each one's NARROWEST station, and is in the blocking gate — PINE VALLEY's own minimum is 5.42 u, so this was roster-wide, not one world. RECORDED LATE: this sentence should have been entered here as OPEN before any code was written against it; it sat in the task list from r411 instead. | FIXED AND GATED r424. FOLLOW-UP CLOSED WITHOUT A CHANGE (r425): the gate's first r424 run showed a car reach 3.97 u from a 1.54 u seat, which I reported to the owner as a seated car sliding off a cambered shelf. THAT WAS WRONG, twice — first at a claimed 2.4 u, then as a camber story at all. Twelve identical resets at IL VICOLO's narrowest station: TEN did not move by a single unit (drift 0.00, zero stations travelled), one settled 0.4 u INWARD, and the one that moved travelled 39 stations (~156 m) with rival FLATSIX measured 4.4 m away at the moment it first moved and 1.65 s of respawn invulnerability still on the clock. No camber moves a car 156 m down a road: it was hit. Invulnerability prices hull, not momentum, and a car parked on the racing line is an obstacle — correct behaviour, and the probe's scenario (leave the car standing for 11 s) is not something a driver does. NO DEFECT, NO CODE CHANGE, and the drift line in tests/test-reset.mjs stays INFO rather than becoming an assertion. |
| E-23 | "I don't see any racing next to a sea with boats in marina etc" + "I want to race NEXT TO it" (owner, 2026-09-13) | CLOSED r429, AND THE HEADLINE NUMBER I GAVE THE OWNER WAS WRONG — recorded in full so it is never quoted again. FOUR THINGS I REPORTED WERE INSTRUMENT ARTEFACTS, NOT WORLD FAULTS. WRONG #1: "only 2 of 77 worlds build a marina" — the static census matched `quay: true` against the nearest preceding theme literal, but themes are assembled by SPREADING a base ({...THEMES.medterrace, quay: true}), so an inherited flag is invisible to a source regex; the runtime shows quay TRUE on 14 of 16. Same class as the r414/r420 post-hoc trap: a literal-only parse of track.js is wrong by construction. WRONG #2: the first distance census printed "sea? none" for every world — my guessed field names (T.seaLevel/T.waterY) matching nothing, not an absent sea. WRONG #3, THE BIG ONE: "median 314 to 1145 u, 0% of the lap within 40 u on TEN of sixteen, MOUNTAIN TO SEA never within 697 u, best is SAPPHIRE SHORE at 12%". Every one of those figures came from the distance to the SEGMENT `T.coast.a`->`b`. The authored segments run ~2.5 km, the laps run off their ends, and the clamp charged those stations the distance to an ENDPOINT. HILLTOWN STACK read 1% where the game's own half-plane says 39.9%. WRONG #4: the half-plane that replaced it is right about the LINE and wrong about the WATER — the sea is a plane at `C.level` and water is only where ground is below it, and `_blendHeight` holds the ground at road datum inside 15 u, so the real shore always stands off the line. THE HONEST INSTRUMENT (tools-scratch/dbg-seawater.mjs, ruler 3): march out from the road edge to the first point with `terrainHeight < level + 0.6` — what is drawn blue and what drowns a car. MEASURED AUTHORED BASELINE, % of lap within 40 u of real water: PORTO GRANDE 50, ALBAROSA SEAFRONT 48, PORTO MOLO 45, WINDWARD COVES 43, HILLTOWN STACK 37, WHITEWASH BAY 30, CITADEL BAY 29, SAPPHIRE SHORE 28, LIMESTONE COAST 22, HARBOR QUAY 18 — TEN WORLDS ALREADY RACED BESIDE THEIR SEA. THE REAL DEFECT WAS FIVE WORLDS: MOUNTAIN TO SEA 0% with NO water within 600 u of any station on the lap, CAPO VELA 0, OLIVE COAST 4, SEA CLIFF RUN 4, CLIFF KNOT 6. FIX SHIPPED (r429, `_pullCoastToLap`): the coast is a line, so sweep every heading at 1 deg, score each on the band it actually produces, and enter the AUTHORED line unchanged as candidate zero so the fit can never return worse than its input; `coastGapU` (34) is a target seat, never a shove, capped by the world's own authored closeness so the quay worlds keep their waterside road. Route, gates, props and AI untouched. RESULT on ruler 3, before->after: HARBOR QUAY 18->42, ALBAROSA 48->58, PORTO MOLO 45->56, PORTO GRANDE 50->57, SEA CLIFF RUN 4->18, OLIVE COAST 4->11, CLIFF KNOT 6->11, CAPO VELA 0->8, MOUNTAIN TO SEA 0->6 (water within 600 u on 59% of the lap where it had none anywhere); six worlds beat every candidate with their authored coast and were left untouched; nothing regressed anywhere on either ruler. CAPE OLIVETO having no coast is CORRECT and not a gap — the owner asked for it to "run inside mountains", its tune sets `coast: undefined`, and its scenery tags were changed to MOUNTAIN + FARMLAND in the same change. NO OWNER DECISION IS OUTSTANDING ON IT. STANDING LESSON, third time in this file: ask what a column actually measures before building the third fix against it. | CLOSED r429 || K-22 | An instanced mesh's colour is NOT in its material (found r434, cost 27 builds) | The owner reported "Red crowns fill the frame at the camera" on FALKEN RIDGE at r406. r398's evergreen census cleared it — "pines/firs were ALREADY green on every palette" — because it read MATERIALS, and the carpet's material is #ffffff with every scrap of its colour in per-instance `setColorAt`. A material census sees white and reports nothing; reading the theme source is no better, because the theme's foliage colours really are green and simply are not what tints these crowns. MEASURED on the instances: 30% of FALKEN RIDGE's carpet crowns were hue 0.075 = 27 deg, ORANGE, on an alpine `furka` pass carrying NO SEASON, and the trees on that branch are `twoCone` — the spruce silhouette. That is §7.14 broken on all FOURTEEN alpine/pass/canyon worlds. FIXED r434: the accent becomes the lighter green the default conifer branch already uses (no new colour invented, and the tonal variation is kept). FALKEN RIDGE now 80,047 instances 100% green, zero orange; GLACIER COL 77,873, 100% green. STANDING LESSON: ask the INSTANCES what colour they are. A green material with a red instance colour renders red. | CLOSED r434 |
| L-4 | Autumn carpet is 43% off-green on a conifer silhouette — owner decision, not taken (r434) | The same paint function tints autumn themes off-green: LARCH GOLD 57% green / 19% orange / 17% red / 7% deep crimson, HARVEST RUN 58/18/16/7. Strictly §7.14 says the cone silhouette stays green everywhere, but both worlds declare `season: AUTUMN`, both are r365's autumn enrichment which the owner asked for and signed off, and LARCH GOLD is named for the one conifer §7.14 exempts by the owner's own word. Turning 43% of an autumn wood green is a design decision, not a bug fix. RAISED WITH THE NUMBERS, NOT TAKEN. (The crimson band is hue ~355 wrapping through `(0.985 + ...) % 1`; my banding filed it as purple. The trees are not purple.) | OPEN, OWNER DECISION |
| K-20 | F7 grass readings on GLACIER COL were never about the grass (found r433) | The suite's `runOne` records top speed ONLY on frames where the ground 4 u ahead is within 3% grade, and re-seats the car at its station every 90 frames, so a run replays roughly the first 24 u repeatedly. The candidate search ranked on a coarse 20 u `runwayGrade` and on brush and NEVER on the fraction of that stretch its own vTop filter would keep. MEASURED, two flat trunk-free solid-free grass runways on the same world and the same surface: 5@12 is 100% flat by that test and reads 128 km/h (67% of road); 885@-12 is 55% and reads 47 km/h (25%). Half the frames were discarded and the surviving maximum reported as "the surface". Any change that moves the scatter re-rolls which wins the sort — which is what the GLACIER COL waiver always meant by "the corridor pick is load-dependent". FIXED r433 by ranking candidates on the flat-frame fraction the filter will accept (>=90%, falling back with the shortfall stated in the verdict line); GLACIER COL now reads 69%, PINE VALLEY unmoved at 59% on the runway it always picked. This is also the honest answer to K-15. FOUR THEORIES DIED FIRST, each by measurement: trunk threshing (zero trunks on both), the ground (1.0 vs 1.1 u of rise over 60 u), solids (zero hits; the search reads camTrees and never `this.solids`), and the surface — where I printed `surf: '?'` thirteen times from guessed method names and did not stop to look at it. | CLOSED r433 |
| K-21 | LAW 4 measures a part's BASE and raycasts down; a bare origin-to-ground gap does not (r433) | While chasing the GLACIAL PASS `element-box` at 8.83 u, my probe reported a worse 12.08 u box — which has 13 element parts within 6 u descending to 5.35 u BELOW grade. It is a building with a 12 u roof, and the suite correctly never reported it, because LAW 4 measures each part's base and then raycasts down, so a roof with a wall under it scores a small gap. The suite was the finer instrument and the probe the crude one. Recorded so the next probe of this class starts from the ray, not from the origin. | CLOSED r433 |
| E-26 | "Full sweep on the track names" (owner, 2026-09-14) | CLOSED r432. Settles the question that had been open since r422 (whether to sweep the other ~70 legal names). RULE APPLIED, so the calls are checkable: rename when the exact string is a PROPER NOUN naming a real circuit, settlement, mountain, region, brand or driver; keep when it is a common noun or plain descriptor, even where a real place happens to use it — the alternative is renaming the language and ending with a roster of mush. NINE WORLDS AND ONE REGION RETIRED: PENEDO LEAP -> KESTREL LEAP (a real city twice over), SAFARI PLAINS -> LONGGRASS PLAINS (the WRC Safari Rally), RED CENTRE RUN -> OCHRE HEART RUN (central Australia), ALPENRING -> CRESTRING (the Alps + the Nurburgring suffix), WALDSCHLEIFE -> GREENSPUR CIRCUIT (a near-copy of Nordschleife), RAZORBACK MOUNTAIN -> RAZOR SPINE (Mount Razorback; Razorback NSW), TOUR DES CAPS -> THE HEADLANDS (the Tour de Corse pattern), TORRI CORSA -> CAMPANILE RUN (Corsa is a car model, i.e. a BRAND), COL DE VERNAY -> COL DE SARANNE (a real French commune), and the region OUTBACK -> BACKCOUNTRY (the Australian region, and also a car model). FOUR CONSIDERED AND KEPT, recorded so the reasoning can be argued with: CORNICHE (a common noun for a coastal road), AUTODROMO VELOCE and VINEYARD VELOCE (two ordinary Italian words), CAPO VELA and IL VICOLO (cape, sail, alley). `col` and `autodromo` are common nouns; `Vernay` and `Corsa` were not — that is the whole distinction. NOTHING RENAMED HERE WAS OWNER-NAMED; SERPENT PASS, which was, is untouched. THE PROOF: the nine retired names were ADDED to test-naming's PROTECTED list, so the suite now scans the live roster for exactly the strings this build removed. It passes at 91 roster entries, zero protected names — a pass only reachable if every rename took. Added, never substituted: an earlier sweep of mine overwrote CINQUE TERRE in that list with one of its own new names, which is how a guard list quietly stops guarding. K-19 FOUND ON THE WAY: `src/world/levels.js` is a SECOND full roster with DIFFERENT ids (COL DE VERNAY 22 there, 21 in the live table). Asked rather than assumed — NOTHING imports it; `src/world/` is ~355 KB of an abandoned refactor and the live roster is LEVELS in src/track.js. The renames were mirrored into the dead copy so a repo grep is clean and it now carries a header saying what it is, but THAT IS A STOPGAP: renaming inside dead code makes dead code look alive. Deleting src/world/ wants its own change with its own gate, not a naming sweep as cover. OPEN for the owner. | CLOSED r432 |
| E-29 | "This is really bugged" (owner, 2026-09-15, phone frame: MOUNTAIN TO SEA, r432, 7TH/8, LAP 1/1, 1:21.1, 104 km/h, hull 207, an OVERTAKE! +38 toast on screen) | RECORDED BEFORE CODE. WHAT THE FRAME SHOWS, described before it is explained: the lower ~75% of the screen is ONE flat surface in a large basketweave/herringbone sett pattern, running from the bottom edge up to roughly the horizon line, with green showing through the gaps down the left side. THE PLAYER'S CAR IS NOT IN THE FRAME AT ALL. Above that surface the world looks correct: cliff face, conifer stand, a red-roofed hut, a truck, blue sky. The pattern coarsens toward the bottom and refines toward the top, i.e. it is a plane in perspective, not a full-screen overlay. THE RACE IS FINE — position, lap, hull, speed, score and the overtake toast are all live and sensible — so this is a RENDERING/CAMERA fault, not a race-state fault. THREE CANDIDATE CAUSES, to be separated by measurement and not by taste: (a) CAMERA — the eye has dropped to or below deck level and is looking along the road surface, with the car behind or inside the near plane; (b) SCALE — a sett/cobble road texture is being built or repeated at a huge world scale on this world, so ordinary geometry reads as giant paving; (c) A NEAR MESH — some other surface (plaza, deck underside, quay) sits between the camera and the car. MEASURE FIRST: reproduce on MOUNTAIN TO SEA and log, per frame, camera height above the road, camera-to-car distance, whether the car is inside the frustum, and what mesh the camera ray hits first. WORKING RULE 3 IS LIVE: the camera on this world has been worked twice (r427/r428 bore glide, #116) and the deck/underside class was named in §3.8, so if the first suspect has been touched before, look for the second code path. NOTE THE BUILD: the owner is on r432 and r435 is live — #125 (stale service worker) means the frame may predate fixes; confirm the fault still reproduces on HEAD before attributing it. | OPEN |
| E-30 | "Gothard pass is gone - that one was fun" (owner, 2026-09-15) | RECORDED BEFORE CODE. THE ONE FACT ESTABLISHED SO FAR, and it does not settle the sentence: world id 19 is still on the roster and still themed `pass` — it was RENAMED to KARVEN CLIMB at r334 ('every stage wears its own name', §7.10/R9), not deleted. Its theme block still describes the flagship pass verbatim: eight stacked hairpins up a granite face, dry-stone retaining walls on every outside edge, an alpine village round the start, pine thinning to bare rock with altitude. TWO READINGS, and I am not guessing between them: (a) THE NAME — the owner misses the word, and 'gone' is literal about the label. This collides head-on with §7.10 and with the owner's own E-26 ('Full sweep on the track names'), so it is a rule conflict for the owner to settle, not a rename to quietly perform. (b) THE CHARACTER — 'that one was fun' is past tense about how it DRIVES, and the rename is 100 builds old while the complaint is new, which argues for (b). Between r334 and now the world has been through the 2x lap stretch (r340), HRD-2 curvature widening, W-CURVE-01, the ELEV_MANDATE grade-lawful compression and the r404 elevation/snake work — every one of which can flatten a switchback stack without anyone deciding to. THE MEASUREMENT THAT DECIDES IT, and it is cheap because the old numbers were written down: track.js:59 records GOTTHARD at 22.9% of lap under a 40 u corner radius and 11.8% under 25 u. Measure KARVEN CLIMB's radius distribution and its height gain on HEAD and compare. If the tight fractions have collapsed, the hairpins are what went and reading (b) is the build; if they still stand, the world drives as it did and the question is purely the name, which goes back to the owner. MEASURED r436, AND READING (b) IS CONFIRMED — BUT THE FAULT IS THE WHOLE ROSTER, NOT THIS WORLD. KARVEN CLIMB against the recorded GOTTHARD figures: 22.9% of lap under a 40 u radius -> 0.0%; 11.8% under 25 u -> 0.0%; tightest corner anywhere on the lap 48.9 u by the game's own curvature array, against a mountain template that specifies 18 u hairpins. The eight stacked hairpins its theme block still describes IN THE SOURCE do not exist in the geometry. Elevation: 425 u of climb and 425 u of descent with a total range of 425 u and exactly ONE reversal of 1.5 m in 8.5 km — a single monotone ramp up and a single ramp down, which is the 'straight vertical-feeling ramp' that 7.17(b) and master-spec T-04 prohibit by name. CENSUS OF 24 WORLDS (tools-scratch/census-curvature-r436.txt): ALL TWENTY-FOUR read 0.0% of lap under 40 u on the game's own array. Minimum radius by that array runs 48.9 u (KARVEN, the TIGHTEST on the roster) to 112.7 u (CANYON RUN). Median radius reaches 2204 u on KESTREL LEAP and 2149 u on CANYON RUN — those laps are very nearly straight lines. On the short-arm ruler only four worlds retain ANY sub-25 u station at all: SUMMIT CLIMB (7 runs, 1.0% of lap), GLACIER'S GRIND (3, 0.4%), OASIS AMBUSH (1) and SERPENTINA DESCENT (1). ROCKFALL RAVINE, on record as the HARDEST track in the game at 25.8% under 40 u, now has no corner tighter than 72.1 u. Elevation is the same story: 18 of 24 worlds have a total road height range under 32 u, and every one of the six that climbs (KARVEN 425, COL DE SARANNE 438, AVALANCHE ALLEY 433, FALKEN 428, SERPENTINA 375, SUMMIT CLIMB 337) has climb EQUAL to its range — one ramp up, one ramp down, no undulation. THREE INSTRUMENT ERRORS ON THE WAY, recorded so the numbers can be trusted: ruler 1 used probe-radius's 6-STATION arm, and since r340 doubled every lap that arm spans 113 u of road and cannot see a hairpin at all — the tell was that the CONTROL worlds read zero too; ruler 2 fixed the arm at 14 u, still longer than the ~22 u of arc a 7 u hairpin turns through; ruler 3 is a 4 u arm cross-checked against the game's own `curvature` array (the one r394 fixed to walk the real arc, and the one the AI steers by), and the two agree. Elevation reversals are COUNTED against a 1.5 m threshold, not inferred from two totals that happen to match. THIS IS ONE FAULT WITH FOUR OPEN TICKETS ON IT: #107 (T-01 zero-curve worlds), #122 ('add more curves and jumps'), #109 and #111 (lower elevation range + snake the climbs, reverted and never re-landed). CAUSE NOT YET ATTRIBUTED, and no code until it is: candidates are r340's 2x lap stretch (scaling a route in XZ doubles every radius), the kink relaxation and arc-length resample, and the `_mandLawful` grade filter's 6000 relaxation passes for the flattened profiles. Reading (a), the NAME, is untouched by any of this and remains the owner's call, since restoring 'Gotthard' contradicts 7.10 and the owner's own E-26 sweep. NOTE: the stale 'GOTTHARD CLIMB' comments in main.js:1195 and track.js:3478 are leftovers from the rename and are not evidence that anything still ships under that name. | OPEN |
| E-31 | "Races with sea in it needs to really be next to the sea" (owner, 2026-09-15) | RECORDED BEFORE CODE, AND IT IS THE SECOND REPORT OF THE SAME THING — E-23 was exactly this sentence in other words ('I want to race NEXT TO it') and was CLOSED r429. Working rule 3 therefore applies in full: a fix that failed twice has a second code path, and r429's `_pullCoastToLap` is not to be tuned again until the second path is found. WHAT r429 ACTUALLY DELIVERED, from my own census rather than from the claim I made at the time: every coast world now has a MINIMUM sea distance of 34 u — the water touches the lap — but the median stays 390 to 1198 u and the share of the lap within 40 u of water is 1 to 10% on thirteen of fifteen worlds (MOUNTAIN TO SEA 1%, SAPPHIRE SHORE 2%, WHITEWASH BAY 3%, CITADEL BAY 3%; the best are HARBOR QUAY 20% and SEA CLIFF RUN 10%). So r429 optimised the metric it was given — it pulled the shore until it TOUCHED the lap at one point — and the owner is describing the other 90% of the lap, where the sea is half a kilometre away or behind a ridge. THE DIAGNOSIS THAT FOLLOWS, to be confirmed not assumed: the fault is in the OBJECTIVE, not the solver. 'Next to the sea' is a property of the whole lap, not of its nearest point, and the metric that will be built against must be a SUSTAINED one — a share of the lap within sight of and close to water, over a contiguous run — with the acceptance stated before the code, as §7.16's sightline census already does for OLIVE COAST. ALSO BINDING: §7.16 (the water must be VISIBLE from the coast road, not merely near it — nearness with a hill in the way already failed once) and the r429 lesson recorded under E-23 that a clamped-segment ruler measured the wrong thing and produced a headline number that was an artefact. Measure with the real-water ruler, name the worlds, and state the acceptance first. | OPEN | SECOND CODE PATH FOUND r437, AND IT IS THE COAST MODEL ITSELF. `_coastSide(x, z)` is the signed distance to a SINGLE INFINITE STRAIGHT LINE through two authored points: `coast: { a: [..], b: [..] }`, one cross product, everything seaward of it is water. Every coast world declares exactly that — a and b, nothing else. A LAP IS A CLOSED LOOP AND A STRAIGHT LINE IS TANGENT TO A LOOP AT ONE POINT, which is precisely the census signature: minimum 34 u (the tangent) and a median of 390-1198 u (the rest of the loop curving away from it). r429 did the only thing its model allowed — swept the line's heading and offset to put as many stations as possible near it — so it was optimising a straight line against a closed curve, and NO AMOUNT OF TUNING CAN CLOSE THAT GAP. That is why this failed twice, and why working rule 3 pointed at the right place. THE FIX, one system: the shoreline stops being a half-plane and becomes an OFFSET PROFILE along the coast axis. The authored a-b line is kept as the AXIS; `offset(t)` gives the waterline's perpendicular position at each point along it, taken from the MOST SEAWARD lap station near that t (its road edge plus `coastGapU`), smoothed and rate-limited so the shore cannot kink or fold, and blended back to the authored offset where the lap is nowhere near. A closed loop has two branches at most t values — the seaward leg and the inland one — and taking the seaward maximum is what makes the loop work: the outbound leg shapes the shore and the return leg is ignored. This keeps `_coastSide` O(1) (project onto the axis, one array lookup, one subtract), which it must be because the drowning rule calls it every tick, and it degenerates EXACTLY to today's behaviour when the profile is constant. ACCEPTANCE, STATED BEFORE THE CODE as E-23's failure requires: (A1) share of lap within 60 u of real water rises from 1-10% to >= 35% on every coast world; (A2) the LONGEST CONTIGUOUS run within 60 u of water is >= 600 u on every coast world — this is the clause that makes it a sustained property instead of a nearest-point one, and the clause r429 had no equivalent of; (A3) the 7.16 sightline census does NOT regress — nearness with a hill in the way has already failed once and does not count; (A4) E-32 closes on the same build: at least one boat within 200 u of the lap on every quay world, measured by the same census that found 681 of them at a 380 u median; (A5) the blocking gate stays green, with nothing-floats and nothing-on-road the ones to watch, since the sea mesh and every body moored on it move. If A1 or A2 misses, the number that was actually reached is reported as the result — not re-described as a success. | OPEN, BUILDING r437 |
| E-32 | "Add boats on the water" (owner, 2026-09-15, frame: CITADEL BAY on r436, 1ST/8, LAP 2/3, 2:23.6, 61 km/h — the cobbled coast road runs along a turquoise bay with spectators on the verge and open water filling the right of the frame) | RECORDED BEFORE CODE. THE FIRST THING TO ESTABLISH IS THAT THIS IS NOT A MISSING FEATURE: `_buildMarina()` exists (pontoons, rigged boats, mooring rings, gated on `this.T.quay`), a moored sail flotilla exists with a shared `_boatHull()`, and CITADEL BAY's own roster comment describes 'the quay and its marina along the shore' as the point of the world. So boats are BUILT. The question is why none is in the owner's frame, and there is already a recorded answer for the same class: track.js:10824 records HARBOR QUAY building its marina 63 u away at CLOSEST and 314 u at median — 'the quays and boats were being built correctly, out where nobody drives'. THIS IS THEREFORE PROBABLY E-31 AGAIN, NOT A SEPARATE JOB: if the water sits 390-1198 u from the lap on the median, then everything floating on it is that far away too, and adding more boats to water nobody passes would be building the same mistake twice. MEASURE FIRST, and measure the thing the owner can see: how many boats CITADEL BAY builds, their distance from the racing line, and how many are within sight of the road at all — not how many exist. ONLY IF the census shows boats genuinely absent (or absent near the road while the water IS near the road) is this its own build; otherwise it folds into E-31 and the boats arrive with the shore. CENSUSED r436 ON CITADEL BAY, AND IT FOLDS INTO E-31. The world builds 883 marina/boat bodies, of which `marina-fleet` alone is 681 INSTANCES — a large, finished harbour: 37 quay sections, 99 posts, 33 finger pontoons, 23 mooring rings. WITHIN 200 u OF THE RACING LINE: SIX. All six are `quay-wall` segments at 11 u; not one boat of the 681 is within 200 u of the road, and the median distance of the whole harbour is 380 u. So the answer to 'add boats on the water' is NOT to add boats — they are already built, in quantity, and they are built a third of a kilometre from where the car drives. This is the HARBOR QUAY class recorded at track.js:10824 ('built correctly, out where nobody drives') for the third time, and it is the same root as E-31: the water, the shore and everything moored on them sit far outside the lap. BUILD CONSEQUENCE: adding hulls would be building the mistake twice, so E-32 is NOT its own build — it is an acceptance criterion ON E-31. When the shore comes to the lap the fleet comes with it, and E-32 closes when a census shows boats within sight of the road, not when more boats exist. ONE FIELD NOT TRUSTED: my probe reported `sea:false` and `seaY:null` on a world whose water is plainly in the owner's frame, so those two flag names are wrong in the PROBE, not absent in the world — recorded rather than quoted, because guessed field names have already produced one false census in this project (E-23). | OPEN, folded into E-31 | SCOPE NOTE: standing decision 2 (repair only) is LIFTED for world scope by MASTER C-4, and E-23's own owner sentence asked for 'boats in marina etc' in the first place, so adding hulls is permitted — but 7.3/HRD-1 still bind (nothing in the carriageway), 7.13's conform rule binds any pontoon or jetty that touches ground, and the nothing-floats suite will judge every hull that is not ON water. | OPEN |
| E-33 | "The fence is levitating" (owner, 2026-09-15, frame: SALINE SPRINT on r436, 8TH/8, LAP 1/3, 0:28.2, 130 km/h) | RECORDED BEFORE CODE. WHAT THE FRAME SHOWS: dark blocky barrier units run down BOTH road edges, and on the seaward (left) side they stand clear of the pale salt ground with daylight visible under them — the row reads as a line of cubes hanging above the beach rather than sitting on it. The landward row is harder to judge from this angle and is NOT claimed either way until measured. THE FIRST THING THAT MATTERS IS THAT THE GATE IS GREEN. `test-nothing-floats` (prop grounding <= 0.15 m, floating-mesh sweep) passed on r436 — the build the owner is photographing — so the fault is not merely unfixed, it is UNSEEN, and the instrument is in question before the code is. K-21 is the precedent: LAW 4 measures a part's BASE and raycasts down, and a probe that measured origin-to-ground disagreed with it. Candidates: (a) the barrier class is not in the sweep's registry at all; (b) it IS swept, but its base is computed from an origin rather than the mesh's lowest vertex, so a unit whose geometry is offset upward reads as seated; (c) it is seated to the ROAD datum while standing on ground that has fallen away beside it — which is the 3.8/7.11 road-edge class and would make this a symptom of the road slab, not of the fence. MEASURE FIRST, on HEAD, on this world: for every barrier/fence/wall body, the gap between its lowest vertex and terrainHeight at its own footprint — then ask why the suite's answer differs from that one. ALREADY OPEN AND PROBABLY THE SAME THING: queue #96, 'road-edge stone/barrier builder fix: mesh AND collider move together', and #95's floating-prop residue. If so this is one fault with three tickets and the builder is the single place to fix it. NOTE THE WORLD: SALINE SPRINT is theme `medterrace` — the olive/coast lineage that r430 and r436 both touched — so check whether anything in those builds moved the ground under a barrier that was seated earlier. | OPEN |
| E-28 | "Also focus on the olive trees a bit more definition" (owner, 2026-09-15, same coast frame as E-27) | RECORDED BEFORE CODE. Reading: the olive trees want MORE DEFINITION — a canopy that reads as a tree rather than a soft green blob. Same frame as E-27, so the same worlds: the medterrace/olivecountry lineage, plus riviera/genova/sanremo since r430 gave them the grove. WHAT THE OLIVE IS TODAY, before any change: `oliveDome()` in `_buildForestCarpet` — a SphereGeometry(1.7, 9, 6) roughened 20%, squashed to 0.72 height and lifted to y 2.7, over a CylinderGeometry(0.20, 0.34, 2.4, 6) trunk. Painted `setHSL(0.21 + rand*0.05, 0.24 + rand*0.14, 0.30 + rand*0.13)` — deliberately silvery sage, "sun-bleached, never fir-dark" (r387). THE THREE CANDIDATE CAUSES, to be separated by measurement and not by taste: (a) GEOMETRY — one smooth squashed sphere has no lobes, and a real olive canopy is broken into several masses on a gnarled trunk; (b) SHADING — if the crown is smooth-shaded it has no facet edges to catch light, which is what "definition" usually means in this game's low-poly language; (c) TONE — at 24-38% saturation and 30-43% lightness the crown may simply lack the internal contrast to show its own form. MEASURE FIRST: segment counts and triangle budget per olive, whether the carpet material is flat- or smooth-shaded, and a rendered close-up — the E-27 lesson is that the authored value and the pixel are different questions. BUDGET IS A CONSTRAINT: these are INSTANCED carpet trees at ~8,500 per olive world (r430 census), so any geometry added is paid 8,500 times; the r375 note puts the whole carpet at a four-draw-call, sub-500k-triangle bill. Definition must come from shape and shading that cost little, not from subdividing the sphere. BUILT r436. MEASURED FIRST, and neither usual suspect was the cause: the carpet material is ALREADY flatShading:true and the crown is ALREADY a faceted 9x6 sphere — the facets existed, there was nothing for them to describe. Photographed at the verge: one squashed sphere reading as a boulder, over a trunk 0.20-0.34 u thick under a 3.4 u canopy, invisible at eye height. BUDGET: 59,663 carpet instances and 2,406,334 triangles on OLIVE COAST, so three lobes instead of one ball is the obvious answer and the wrong one. FIX AT THE SAME COST: the crown is LOBED rather than subdivided (each vertex pushed in and out radially by a low-frequency function of its own angle, so the same sphere grows three masses and the notches between them) and the trunk is shorter, thicker and flared (0.34 -> 0.62 base over 1.9 u). Triangle count after: 2,406,334 — identical to the digit. HONEST LIMIT: it reads at mid distance, which is where the chase camera sees the verge; at point-blank the crowns stay chunky, which is inherent to a 9x6 sphere. Only the olive lineage is touched; the conifer carpet is a different geometry on a different branch. | CLOSED r436 |
| E-27 | "Make the sea looo more imposant" (owner, 2026-09-15, with a coast frame) | RECORDED BEFORE CODE. Reading: make the sea look more IMPOSING. The frame is a cliff-top coast road — olives, cypress, cobbles — with the water filling the upper right and reading as a flat pale sheet: no swell, no surface detail, no sun path, no surf, and a wash toward near-white at the horizon. SCOPE IS THE WATER SURFACE, ONE SYSTEM: colour and its depth falloff, surface detail, and the specular/sun path. The shoreline surf line is a DIFFERENT builder (beach/quayside) and is not in this build unless measurement says the surface alone cannot carry it. THE HISTORY THAT CONSTRAINS IT, so this does not undo two earlier owner fixes: r387 answered "I don't see any sea in olive coast" and r403 answered the cream wall, and the per-vertex fade from `seaColor` at the beach toward the sky-horizon tone EXISTS BECAUSE the first sea faded into a cream wall under scene fog. Imposing must therefore come from the NEAR and MID water — depth of colour, movement, light — and MUST NOT be bought by darkening the horizon back into a wall. ALSO BINDING: standing decision 2 is lifted for world scope by MASTER C-4, so this is permitted; the HUD freeze (standing decision 3) is untouched; §7.16 (the sea must stay VISIBLE from the coast road, sightline census) still holds and is the regression risk if the water gains height or the band gains mass. BUILT r435, TWO FAULTS. (1) `cFar` was `seaColor.lerp(skyHorizon, 0.8)`, and on a warm Mediterranean palette the sky horizon IS a cream — r387 moved the SOURCE of the cream, not the cream. Computed across the roster the far water was under 25% saturation on EIGHT of fifteen coast worlds and outright yellow on two (CAPO VELA hue 67 at 15%, SEA CLIFF RUN hue 58 at 21%, HILLTOWN 9%, WINDWARD 10%). Now built from the sea, borrowing only the sky's LIGHTNESS: sampled from the built geometry, all fifteen read hue 196-216 at 42-66%. It still lightens INTO the sky, so r387/r403's no-wall requirement holds. (2) AND NONE OF THAT REACHED THE SCREEN. The sea was a MeshStandardMaterial, so every authored vertex colour was multiplied by a warm sun at intensity 2.6 and blew out; the old note on that material (matte 0.7 because 0.14 'washed the whole bay to one flat cyan') was fighting the same fire from inside it — BOTH ends of the roughness range wash, because the problem is the diffuse multiply, not the specular lobe. MEASURED IN PIXELS, sea band vs sky band in the same frame: OLIVE COAST sat 13%->55% and lum 60->37 (it was 31 points BRIGHTER than the sky), CAPO VELA 19%->66% and 55->36, LIMESTONE COAST 36%->88% and 43->30. The water is AUTHORED, not lit — gradient, haze, facet jitter and bob are all baked — so it is drawn UNLIT with the world's hour baked in as a sun/sky tint normalised to unit luminance: carries the light, never brightens the water, and costs less than lighting a huge mesh per pixel. NOT MEASURED, AND SAID SO: the fourth probe world (SEA CLIFF RUN) returned identical numbers to the decimal before and after, which is never a result — its camera faces a cliff and the fixed sample rectangle sits on rock. Three worlds measured, the fourth reported as unmeasured rather than folded into the win. AND ONE CORRECTION TO MY OWN FIRST READING: I sampled the owner's frame at the horizon, called it sea, and blamed cFar for all of it. The pixels say that warm band is the SKY DOME — each world's authored hour — and it is left alone. | CLOSED r435 |
| E-25 | "Don't auto reset unless I press sos" (owner, 2026-09-14) | RECORDED BEFORE CODE. OPERATIONAL RULE: the PLAYER has NO automatic return. Every free auto-return is deleted and the SOS/UNSTUCK button is the only thing that puts the player back on the road. This is the endpoint of a direction the owner has now set three times — r345 deleted the missed-gate return, r364 deleted the cliff-top/off-road return, and this deletes the physical-trap returns that both of those explicitly kept. It also SETTLES R-RECOVER-01's held trigger 2 in the opposite direction to the patch: not only no off-spline auto-reset, no auto-reset at all. SURVEYED BEFORE WRITING ANYTHING — the player has exactly THREE automatic returns and they are in two files: (1) main.js the void watchdog, chassis below `belowTerrainM` sustained past `voidConfirmS` -> returnToGate reason `void` (this is CLAUDE.md §3.2); (2) vehicles.js `_lostT > 2.5`, more than 6 u under the drawn ground OR a non-finite coordinate; (3) vehicles.js `_wedgeT > stuckDetectS`, wedged with the throttle held. There is NO upside-down auto-return in the player path (it survives only in comments), and `_deepStuckT` is the RIVALS' pit-lift, not the player's. WRECKS ARE NOT RESETS AND ARE NOT TOUCHED: the bog rule (owner: "car gets wrecked after no successful trial of 5s"), drowning, and fatal falls (§3.4b) are DEATHS the owner asked for, with their own hull economy; deleting them would undo three separate directives. RIVALS KEEP THEIR RECOVERY unchanged — §3.6c's "a parked rival is a bug" stands and the owner's sentence is about the player ("unless I press sos"). SAFETY PROPERTY VERIFIED BY READING, NOT ASSUMED: SOS is gated on `controlsLive = g.state === 'race'` only, never on position, so it stays pressable while under the terrain, airborne, or submerged. Without that this change would be a soft-lock. ONE RECORDED DEVIATION, raised rather than taken silently: the NON-FINITE coordinate half of trigger (2) is KEPT. A NaN position is not a place the player can choose to leave — the car does not render and no SOS decision is meaningful — so it is treated as state corruption, not as an auto-reset. If the owner wants that gone too, say so and it goes. BUILT r431: all three deleted for the player, rivals untouched, wrecks untouched. VERIFIED: car held 30 u under the terrain for 6 s -> 0 automatic returns, one SOS press -> back to -0.6 u of the surface; wedged 5 s -> 0 automatic rescues, button 0 -> 1. TWO FALLOUTS FIXED: the void log re-armed every half second once nothing cleared it (282 records in 6 s) and now latches once per burial; test-killspos P2 encoded the OLD law (rescued at ~2.5 s) and was rewritten to the new one rather than worked around. AND THE PROBE LIED FIRST: it counted `void` telemetry records — the thing deliberately kept — and reported 282 'automatic returns'. Count the action, not the log. | CLOSED r431 |
| E-24 | "The olive and wine grows needs to be more dense where applies" (owner, 2026-09-13) | CLOSED r430. THREE FAULTS, AND ONLY THE THIRD IS THE ONE ASKED ABOUT. (1) "WHERE APPLIES" WAS FOUR WORLDS SHORT: `CARPET_THEMES` and the carpet's `terrace` test both matched the theme NAME, and `riviera` (plus `genova` and `sanremo` through it) DERIVES from `medterrace` by spreading it — riviera's own comment says medterrace "already has olive hills right". They inherited every field except membership of two string sets, so the whole ALBAROSA COAST grew NO grove: measured 0 carpet instances and 185-593 solid trees against the terraces' 5,600 + 845. Fixed by putting the flag on the theme (`grove: 'olive'`), which spreading carries and a name never did. THIRD BUILD THIS TRAP HAS COST (r414, r420, E-23). (2) THE GROVE PLANTED INSIDE BUILDINGS: enabling it on the derived themes put olive domes through the pantile roofs of ALBAROSA SEAFRONT (screenshotted before and after, tools-scratch/shot-grove-ALBAROSA-18.png). The ring tested road, water and slope and nothing else. Now every spot is tested against `this.solids` through a coarse 48 u grid (a flat scan is ~117M tests on a town world). NOT a riviera problem: rejections per world 1,140-4,397 (and GLACIER COL 3,763 against PINE VALLEY's 602), i.e. the TERRACES had been growing trees through their own huts all along. A REJECTED SPOT IS NOT RE-ROLLED INTO THE OPEN GROUND: the first cut skipped the sample and the loop made the shortfall up elsewhere, keeping the same tree count in less ground — which pushed GLACIER COL's carpet onto its open grass and took its last acceleration runway (F7 grass 0-30 1.18 s -> 11.98 s, a hard block on the gate). Road/water/slope rejections still retry (a bad SAMPLE); a solid is occupied ground, so the tree comes off the budget and density is per unit of PLANTABLE ground. GLACIER COL's 0-30 back to 1.7 s. SCOPED TO OLIVE COUNTRY ON PURPOSE: the test is right everywhere (GLACIER COL alone has 3,763 spots inside solids against PINE VALLEY's 602) but applying it roster-wide moves every carpet instance on every carpet world, and the gate returned two worlds red that this build has no business touching — F7 grass on GLACIER COL and a floating element box on GLACIAL PASS (8.83 u vs 8). Both are marginal cases a reshuffled scatter flipped. That is HRD-7's roster sweep (#120) and needs its own build with the fallout budgeted. Scoped, every non-olive world is bit-identical to r429. HRD-7 closed FOR THE CARPET IN OLIVE COUNTRY; the rest of the roster and the other tree systems stay open. (3) DENSITY: olive country was planted at ~half woodland (4,200 + 7,000 attempts vs 9,000 + 14,000). The GROVE band (38-160 u) goes to 13,000; the VERGE band (1-38 u) is UNTOUCHED, because that is the wall r413 removed and the terraces already carry 700-1,070 inside 20 u. Measured grove band 3.4-3.8k -> 5.9-6.4k (+65 to +75%) with the inside-20 u count DOWN 1-6% on every world; the Albarosa coast 0 -> 5,988-6,502. WINE: `count: 85` was NEVER the density. Instrumented before tuning, FOUR PARCELS IN FIVE were thrown out by the flatness gate (VINEYARD VELOCE planted 18 of 85, 67 rejected there; BRIDGE RUN 14 of 85, 71) and nothing was rejected for any other reason. The gate was 3.4 u over 11 u — a 17 deg limit — on worlds r378 REQUIRES to have 500 m of vertical: it refused to plant on the land the world is made of. At 7.0 (~32 deg) parcels go 18->32 and 14->24 and instances 9,949->15,373 (+55%) and 7,036->9,935 (+41%). Band, parcel gap and grade are now named per-theme handles. VINEYARD WORLDS GET NO OLIVE CARPET ON PURPOSE — vine country, and they already carry 4.3-7.8k solid trees plus the rows; their solid counts moved only because more vine parcels shift the shared Math.random() stream (a side effect, not a result). TWO HARNESS BUGS FOUND WHILE TRYING TO LOOK: a hand-posed `g.camera` is overwritten by the game's own rAF render before the screenshot lands (six frames of blurred dirt that read as "the grove is missing"), and `placeAt` does NOT seat vertically (car at y 9.4 under terrain at 410 on VINEYARD VELOCE). The probe now uses the rescue flag and EXITS 1 rather than save a frame whose car is not on the ground. | CLOSED r430 |

## K. Recorded conflicts and their resolutions

Never resolve one of these silently. Each was found by reading, not by a bug report.

| # | Conflict | Resolution |
|---|---|---|
| K-1 | Elevation mandates (passes 1200 m, terraces 500 m) vs WR-2.1 grades 4-12% plus a 5.5 km lap. A lap climbing AND descending H at grade g needs length ≥ 2H/g, so 12% at 5.5 km caps the range near 330 m. | Master spec is newer and higher: ELEV_MANDATE compresses to the grade-lawful range per world. Recorded, not silently traded. |
| K-2 | R-RECOVER-01 trigger 2 (>15 m off spline → reset) vs C-1 and C-2. The patch's precedence clause defers to the MASTER SPEC, which does not cover these owner overrides, so the clause cannot settle it. | Triggers 1, 3, 4 shipped; trigger 2 HELD (L-1). The patch's own acceptance case (3-13 km/h) is met without it. |
| K-3 | R-RECOVER-01's 40 km/h rolling start vs C-3 ("a restarted car STANDS"). | HELD (L-2); C-3 kept meanwhile. |
| K-4 | G-AI-01 asks to bound a rubber band vs H-1, which deleted it in r313. | No band exists to bound. Root-cause the observed bunching instead; H-5 is the prime suspect. HELD (L-5). |
| K-5 | B-8 (trees cannot be pushed) vs §1's "smash and shove props with score". | The rule wins; tree-smash scoring goes. Flagged to the owner when shipping. |
| K-6 | W-EDGE-01a's 4 m band vs §7.11's deliberate 3 m verge setback. | The band as written measures the verge, not the fall, and misses both observed falls. Derive the band from measured sideways travel after a hit. OPEN (L-3). |
| K-7 | Three different drop thresholds: W-EDGE-01a says >2 m within 4 m; the shipped rail builder uses 2.5 m at a single 7.0 m probe; §7.11 says any step over 0.15 m at the road edge. | Unreconciled. Must be settled as one number before the audit is wired into the gate. |
| K-8 | The owner titled the Race Integrity patch RALLY_PATCH_02, but `RALLY_PATCH_02.md` already holds the 2026-09-06 document RALLY_MASTER_SPEC.md supersedes. | Filed as RALLY_RACE_INTEGRITY.md so neither is lost. Cite it as RALLY_PATCH_02 (Race Integrity, R21). |
| K-9 | Adding tiers above HARD vs three sites testing `difficulty.id === 'hard'` as shorthand for "top tier" (a contract gate, the rung ladder, a 1.25× payout). | Left alone, SAVAGE would pay like NORMAL and lock the hard rungs. All three compare `tier` now. |
| K-10 | Switching `#speed-lines` off vs r309, which lowered its threshold to 95 km/h for the owner sentence "do not feel I go 70". Both are owner directives; the later one ("Still see the lines", after four earlier reports of the same artefact) is about the same pixels. | The later sentence wins on the overlay. The r309 INTENT is not abandoned — the FOV push and camera pull already carry the sense of speed — but it will not be carried by white lines the owner keeps filing as a defect. `speedLinesFromKmh` set to 9999 (above any reachable speed) rather than deleting the element, so a future implement can re-enable it deliberately with a design that does not read as a sliver. |
| K-11 | "I don't see an order how do I win 1st place or 4th place" vs r363's REVERSE CHAMPIONSHIP GRID ("I should not be starting 1st always"), which deliberately puts the title leader at the back. | Not traded. The measured fault in the same sentence — 230 m of grid — is enough to explain an unreadable order on its own, and it is fixed (D-6). The reverse grid itself is left exactly as the owner asked for it in r363; whether it now reads as intended is a question for them (L-6), not a decision to take while fixing a units bug. |
| K-12 | "Add hazards and obstacles to Dodge and blow up across the tracks" (E-9) vs HRD-1 "RULE: nothing stands at the middle of the road", which is a HARD rule enforced by the blocking gate (test-nothing-on-road), and vs §7.3's 4 m exclusion band. Read literally, "obstacles across the tracks" means bodies in the carriageway, which is the thing the owner separately reported as a defect at r361. | Resolved by CLASS, not by exception: HRD-1 forbids a body that STANDS there. A hazard that ARRIVES (rockfall, rolling boulder, crossing traffic) or that the player DESTROYS is not a standing body, and neither is a destructible prop outside the 4 m band. No static obstacle is added to the racing line and no by-name gate waiver is taken. Flagged to the owner in case they did mean literal blockers, in which case HRD-1 needs amending FIRST and the gate suite with it.  OWNER ANSWERED "Yes" (2026-09-10) to the question "did you mean literal blockers in the carriageway", adding "Folow the graphics I send". So HRD-1 IS TO BE AMENDED — but not on my reading of one word. The amendment waits for the first hazard graphic (A-G), so that what lands in the carriageway is drawn against a reference the owner supplied rather than invented here, and so HRD-1 and test-nothing-on-road are changed once, precisely, with the class of body named. Until then no obstacle goes on the racing line and the gate suite is untouched. | RESOLVED 2026-09-10 BY THE FRAME. The owner sent the hazard graphic (annotated r409 CANYON RUN). Read as the brief it names the bodies (E-9) and settles the conflict narrowly: the only NEW class the frame puts inside the carriageway is a FLAT CHARGE PLATE lying on the dirt. CORRECTION, on measurement after this was first written: crates, cones and barrels are ALREADY on the drivable surface on every world — `_buildProps` puts them there deliberately as pass-through smashables and `test-nothing-on-road` counts rather than asserts them. So HRD-1 had an unwritten carve-out all along and the amendment is not opening a closed rule, it is writing the real one down and bounding it. The amendment is therefore ONE typed class with hard bounds (HRD-1a), not a hole in the rule and not a by-name waiver: <= 0.25 m tall, destructible, no velocity stop, telegraphed 60 m, one per 150 m, never in a gate clear zone, apex band, kicker landing zone or the finish run-out. `test-nothing-on-road` asserts those bounds rather than skipping the station. Everything else the gate forbade in the carriageway stays forbidden.
| K-13 | The E-9 hazard frame (2026-09-10) draws FLOATING RED TARGET / CROSSHAIR MARKERS hanging over the rock arches, marking what to shoot. Standing decision 2 says "no new on-screen elements, no world overlays", and it was written to erase exactly this family — the r297 yellow centreline and the rival arrows were floating world-space guides and the owner had them deleted with no replacement. | A-G says the frame outranks my inference, so I do NOT get to quietly drop it; standing decision 2 says "do not reopen", so I do not get to quietly build it either. HELD AND RAISED (L-7). The other five bodies in the frame are unambiguous and ship without it; the destructibles are readable on their own paint (red drums, stencilled crates) with no marker needed, so holding this one element costs the frame nothing. If the owner confirms the markers, standing decision 2 gets an explicit carve-out for diegetic target reticles on destructible bodies, recorded before any code. |
| K-14 | B-9 ("This should not be allowed", a car traversing a steep bank beside the road) requires the steep-face shed law to act INSIDE the 34 u rejoin band. The r328 fence deliberately exempts that band on the reasoning that "banks are moments, not faces", and it protects the rejoin: a car bouncing off a verge back onto the road must not be shed mid-bounce. | The owner frame outranks my inference (A-G), so the band stops being a blanket exemption — but the rejoin case it protects is real and was measured once, so the fence is NARROWED rather than deleted: inside the band the shed needs a grade clearly past the ceiling AND a dwell longer than a bounce, both numbers to be set from a measurement of the frame's bank and of a normal verge bounce, not chosen. If a narrowing cannot hold both, the rejoin case wins and the conflict comes back to the owner. Nothing built until the angle is measured. |
| K-15 | The r410 gate turns green on GLACIER COL only because a WAIVER absorbs the failure, and the waiver does not fit it. The line is `F7 GLACIER COL: grass tops 66 vs 190 km/h = 35%, runway 5@12, NO brush-free corridor (best 1 brushed samples)`. The waiver (tests/validation.mjs:69) matches on `/(PINE VALLEY|GLACIER COL): grass tops/` — A WORLD NAME AND NOTHING ELSE — and its stated cause is "thrust-equilibrium marginal vs [55,75]" with its own ledger recording GLACIER COL at 89% on the r403 base. 35% is not marginal, it is half the bound, and "NO brush-free corridor" is a different failure mode from an equilibrium read. The prime suspect is r410 itself: registering the mid forest ring put colliders and brush where the runway search now cannot find a clean line. | TWO SEPARATE FAULTS, BOTH RECORDED, NEITHER SHIPPED SILENTLY. (a) THE GATE: a name-matched waiver cannot tell the failure it was written for from a new one, so it will hide any future F7 regression on those two worlds. It needs a MAGNITUDE BOUND in the match (waive only within the band the ledger actually measured) so a real regression cannot shelter behind it. (b) THE BUILD: r410 must be A/B'd with the mid-ring registration on and off, on GLACIER COL, before r410 is called good — if registration is the cause then r410 traded a forest-containment fix for an off-road surface regression and the owner is owed that trade, not a green tick. Neither is touched while the gate is running against this tree. | MEASURED 2026-09-10, AND IT CLEARS r410 — MY PRIME SUSPECT WAS WRONG. A/B on GLACIER COL with the mid-ring registration ON and OFF (tools-scratch/dbg-glacierbrush.mjs, the ring temporarily flipped to reg=false and reverted): registration ON gives 2933 registered trees in the runway belt; OFF gives 2896. The mid ring contributes THIRTY-SEVEN of them — the other 2896 are one pre-existing mesh that was already there before r410. And the number that actually decides the corridor is IDENTICAL either way: 63 of 450 sampled stations trip r399's three-trees-within-8u brush trigger, 14.0%, ON and OFF. So r410 did not starve the runway and there is NO trade to report to the owner: the forest-containment fix cost nothing in off-road surface. WHAT REMAINS TRUE: fault (a) is untouched — the waiver still matches on world name alone, so it would still hide a real regression, and 35% is still far outside the band its own ledger records (GLACIER 89% at the r403 base). The remaining explanation is the waiver's own "corridor pick is load-dependent" note: with 14% of stations brushed, whether a 5-sample runway lands brush-free is luck, and that instability is the thing to fix — either the runway search or the waiver bound, not the world. Fault (b) is CLOSED as not-r410.
| K-16 | E-13 "Create farmland themed tracks" vs CLAUDE.md §9 rule 5 ("Nothing is added to any world. World count, chapters and star ratings unchanged") | The owner's word outranks the working spec, but adding worlds silently re-prices the whole career. Resolved FOR NOW by building the farmland tracks out of the existing field-country worlds under the C-4 redesign licence, and telling the owner plainly that the roster stayed at 78 so he can say otherwise. | RAISED |
| K-17 | E-20 "Rename the tracks" vs CLAUDE.md §7.10 (no real circuit, city, mountain, brand or driver names in STAGE DATA) as applied to route/theme KEYS | 15 internal keys are real-world names (turini, pikes, estonia, principality, ardennes, tremola, furka, dolomiti, liguria, aegean, dalmatia, azur, riviera, genova, sanremo). They never reach the screen, and `seedForLevel` hashes `epoch:id:route||theme`, so renaming one re-rolls that world's scatter — every tree, rock and building moves. Renaming them is a world rewrite wearing a rename's clothes, which working rule 1 forbids in a rename build. Keys left as they are; the displayed names, which R9 actually scans, are clean. Raised so the owner can order the re-roll if he wants the keys clean too. | RAISED |
| K-18 | RALLY_RACE_INTEGRITY R-FINISH-01 (P0, "the finish line must end the race") vs what HEAD actually does | MEASURED, NOT ASSUMED (r423). Both root-cause candidates the patch names are refuted: the trigger fires in every state tested — grounded, airborne, drifting, shielded, mid-respawn — at 0 ms (same frame), and still fires with the `controlsLive` veto forced onto the crossing frame; and no rescue on the closing stretch lands past the trigger plane (880->880, 890->896, 895->896, 898->898, 899->899). The input lock holds at 3 m roll-on. The P0 as specified is MET, so no code changed; tests/test-finish.mjs (16 assertions) went into the blocking gate instead, because the live risk is now silent regression rather than the original fault. ONE DEVIATION STANDS: the rule asks for the nearest respawn to sit >= 30 m before the plane, and a rescue at index 898/899 declines to move the car at all, leaving it 15 m and 8 m out — a no-op, not a placement past the line, and dragging a player backwards from a finish they nearly reached would be worse than the deviation. The captured 3:21.7 symptom therefore has a cause not yet found; it is NOT closed by this build. | RAISED |
| J-9 | A DRIVING.JSON KEY WITH NO DEFAULT IS DISCARDED SILENTLY (found and made loud r426) | `loadDrivingOverrides` merges nested blocks with `if (kk in DRIVING[k] && typeof json[k][kk] === typeof DRIVING[k][kk])`. The guard is right — a typo must not invent a constant nothing reads — but it was SILENT, and the working spec says every tuning number lives in driving.json, so adding one there and nowhere else tunes nothing and says nothing. Cost: a whole camera build spent tuning `boreCeilRiseUPerS`, which read back MISSING at runtime. A NEW TUNING CONSTANT MUST BE ADDED IN BOTH PLACES — the default in src/driving.js AND the value in driving.json. The loader now warns once with every ignored key and its reason, and exposes DRIVING.__droppedKeys for probes; proved by negative control (real file 0 drops, planted file reports exactly its 2 faults, a valid key in the same block still loads). Audited at the time: 122 keys reach DRIVING, 0 dropped. | LIVE |
| E-22 | "camera shakes AFTER the tunnel" (owner, r408; his SECOND shake report after r398 "car still shaking") | HALF FIXED r427, HALF STILL OPEN — and the open half is the one his sentence names. NOT the shake system: g.shake is 0 through every affected frame, so working rule 3 was right that the second report has a second code path. ROOT CAUSE of the sustained churn, found by instrumenting camPos.y with an accessor that records its own caller: the in-bore clamp forces the EYE into the bore height band every frame while targetPos (what the chase lerp eases toward) kept the open-air height, which inside a bore is above the roof — clamp -5.46 u down, lerp +4.46 u up, alternating. FIXED by recording the band and applying it to the target BEFORE the lerp (v1 clamped it after, where the object is already spent, and changed nothing). MEASURED KARVEN CLIMB stations 100-110: 146-155 deg/s -> 9-10, bob 1.1 u -> 0.4 u, p99 21 -> 19; ordinary driving is 3. Same fix in the deckOverhead branch, which had inherited the bug along with the clamp shape. STILL OPEN: the single-frame snap at station 113, just past the portal — 594 deg/s, height 3.9 -> 9.3 — which the writer probe attributes to the lerp taking a +4.46 u step, meaning the target is far above the eye there; _camDt at that frame is the next measurement. The approach spike at station 94 also worsened 727 -> 780 (tunnelAt carries a 6-station pad, so the band now applies from 90). Gate blind spot recorded: test-camstable drives level 2, which has NO BORE, and thresholds SUSTAINED angular velocity at 120 — the 145-160 churn would have tripped it on a world that has one. | BOTH REPORTED HALVES FIXED (r427 in-bore, r428 portal). r428 measured the snap at the exit: target 139.2 vs eye 88.34, a 50.86 u gap, with k and camDt both nominal — the eye sat far below a ceiling that DOES bind (93.6) and closed 4.46 u in one frame, ~268 u/s. The ceiling caps HEIGHT and nothing capped RATE, which is also why r426's rise-limited ceiling was inert: limiting the ceiling does nothing while the eye is underneath it sprinting up. Fixed with a climb cap inside the bore window only (boreRiseCapUPerS 22 u/s, declared in BOTH src/driving.js and driving.json per J-9). Station 113: 581-594 -> 45 deg/s. CORRECTION TO THE RECORD: r426 claimed the ceiling "never binds" from a post-run snapshot (_boreCeilY 163) taken wherever the car stopped; at the portal it binds at 93.6 — the revert was right, that reasoning was not. STILL OPEN, and NOT what the owner reported: the APPROACH spikes at stations 90-95 (780/690/584/539 deg/s), the eye DESCENDING as the ceiling falls before the portal. The fall direction is deliberately unlimited because it is what keeps the eye out of the rock, so the entry side needs a smoother descent ramp, not a symmetric cap — its own build. |

## L. Open questions for the owner

| # | Question |
|---|---|
| L-1 | R-RECOVER-01 trigger 2: reinstate the unconditional ">15 m off the spline for 2 s" reset, reversing C-1 and C-2? Shipped without it; the patch's own acceptance is already met. |
| L-2 | R-RECOVER-01: 40 km/h rolling start, or keep C-3's standing restart? Kept C-3. |
| L-3 | W-EDGE-01a: set the lateral band yourself, or shall I derive it from measured sideways travel after a 250 km/h hit at the two known spots? |
| L-4 | E-3: the elevation/weave build is reverted and held pending containment fixes. Re-land when? |
| L-5 | G-AI-01: accept root-causing the bunching (H-5) instead of adding a band to bound? |
| L-6 | The grid is now a real grid (10 m rows, 29 m deep) but still REVERSED by season standings, so leading the championship means starting 8th — your r363 request. With the spacing fixed, does the reverse order stay, or should the grid form the normal way round? |
| L-7 | The hazard frame draws floating red target markers over the arches. Standing decision 2 deleted exactly that family (the r297 centreline and rival arrows) and says do not reopen. Do you want the markers, or are the red crates and drums marker enough? Everything else in the frame is being built either way. |
| L-8 | "Add wide fields scenarios. Those where there and got deleted." I cannot find the deletion: the roster is 78 worlds now and was 78 before, the mountain-flank rule already exempts field roads exactly as you scoped it, and the crop/hedge/stubble builders are all still there. What I DID find is that CLAUDE.md §8 specifies an "open" track template — 8 m half-width, 200 km/h design speed, 60 m minimum corner radius, no boundary — and not one of the 78 worlds is built to it. Is that the wide-field racing you mean (fast, open, huge-radius curves through crop country), or do you mean the LOOK on the existing farm worlds, or the free-roam meadows? | ANSWERED 2026-09-11: the fast open-field tracks (option 1).

---

## Appendix. Where the other documents sit

| Document | Holds |
|---|---|
| RALLY_MASTER_SPEC.md | HUD H-01..03, track T-01..05, containment W-EDGE-01 + W-CURVE-01, scenery S-01..05, vegetation V-01..02, validation VAL-01..03, change control C-01..02. Above CLAUDE.md in authority. |
| RALLY_RACE_INTEGRITY.md | The R21 patch: R-FINISH-01, W-EDGE-01a/02, R-RECOVER-01, C-CAM-01/02/03, G-DMG-01, G-WRECK-01, W-CURVE-02, G-AI-01, W-TEX-01, P-PERF-01. |
| RALLY_W_CURVE_01.md | Standalone copy of the curve containment rule. |
| RALLY_PATCH_02.md | The 2026-09-06 document, superseded entirely by the master spec. Kept for history. |
| CLAUDE.md | Working spec: build order, module map, acceptance queries, §7A HRD table, the recording ledger. |
| RULES.md | World rules and object reference: collision classes and their exact mechanics. |
| DRIVING_SPEC.md / RALLY_DRIVING.md | Vehicle physics constants. |

{
  "name": "dustline",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "verify:track": "node tools/verify-track-format.mjs",
    "smoke:editor": "node tools/editor-smoke.mjs",
    "gate": "npm run typecheck && npm run verify:track"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.12.0",
    "howler": "^2.2.4",
    "three": "^0.160.0",
    "zustand": "^4.5.2"
  },
  "devDependencies": {
    "@types/three": "^0.185.3",
    "typescript": "^5.4.5",
    "vite": "^5.2.0"
  }
}
